# MAX Mod v26.24.0 V20 (RuStore)

**Сток:** 26.24.0 (RuStore, RS variant) · **Совместимость:** Android 8.0–16.0 (API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

Хотфикс поверх V19. **Весь функционал V19 сохранён, добавлен новый тумблер приватности.** Ставится через `install -r` поверх любой сборки 26.x — данные сохраняются.

## 🆕 Что нового в V20 (относительно V19)

### 👻 Скрыть просмотр историй (раздел «Невидимка»)
Новый тумблер `hideStoryView`: другие пользователи больше не узнают, что вы просмотрели их историю — событие просмотра не отправляется на сервер. Сами вы по-прежнему видите все истории и список тех, кто просмотрел ваши.

Работает двумя способами:
- **Свой тумблер** — можно включить точечно, не трогая остальную «Невидимку».
- **Автоматически при «Невидимке»** — тумблер визуально и функционально стал дочерним для мастер-переключателя «Невидимка»: включаете «Невидимку» → просмотр историй скрывается вместе с набором/прочтением/онлайном, без отдельного действия.

---

## 📦 Из V8–V19 — сохранено

- **💬 SMS-код при регистрации приходит** (V18–V19): встроена поддержка подлинных констант криптографического отпечатка клиента (DEX/SO/SIG) — сервер больше не блокирует SMS.
- **🎨 Иконка клона (Orange)** отображается корректно.
- **📞 Гудок дозвона V29**: `stop()` в момент реального ответа собеседника, атомарный `ToneGenerator`.
- **📦 Оптимизация размера APK** (V9): −17.6%.
- **🔔 Фикс повторных уведомлений при Антиудалении** (#PUSH-2): dedup-гейт `antiRead OR antiDelete` + throttle 30 сек.
- **🎨 Цвет уведомления = цвет иконки** (#ICON-1): тумблер `notifMatchIcon` (ON по умолчанию).
- **📌 Больше закреплённых чатов** (#PIN-1): тумблер `unlockPinLimit` (OFF по умолчанию, лимит до 10).
- PIN-блокировка, скорость воспроизведения, watch-фичи, невидимка, антиудаление, AMOLED, 6 цветов иконки, проверка обновлений, дев-меню.

## 📱 SMS-регистрация — работает

WS-redirect от сервера MAX не блокируется. SMS-код доходит штатно.

## ⚠ Известные ограничения

- **Ringback**: ToneGenerator маршрутизируется через STREAM_VOICE_CALL; на устройствах без активной VoIP-сессии в момент DIALING гудок может быть тихим.
- **#PIN-1**: сервер может сбросить пины >2 при синхронизации.
- **AMOLED** — покрытие ~95%.
- **Watch-фичи** активны только при `widthPixels < 500`.
- **Клон V20 не был device-тестирован** в этом релизном цикле (main arm64 — да, чисто). Собран и подписан из того же исходного дерева, что и main, но рекомендуется проверить перед тем, как полагаться на клон.

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.24.0_arm64_V20.apk` | Основной аккаунт, arm64-v8a (большинство современных телефонов). |
| `MAX_26.24.0_arm7_V20.apk` | Основной аккаунт, armeabi-v7a (старые устройства). |
| `MAX_26.24.0_arm64_V20_clone.apk` | Клон arm64. Оранжевая иконка, пакет `ru.oneme.ap2`. |
| `MAX_26.24.0_arm7_V20_clone.apk` | Клон arm7 (для WearOS / OnePlus Watch 3). |

**Клон (первая установка):** после установки — один раз Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS.

## Подпись

- apksigner **v2 + v3** (без v1). Cert SHA-1: `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.
- Smoke: Samsung S25 (`RFCX5013V5F`), Android 15, arm64 (main) — без FATAL / VerifyError.

## SHA-256

```
c4b873b6ee864d564b1f412d0557bb3262bb3aede14e709cbd9a71d884cefd1d  MAX_26.24.0_arm64_V20.apk
e561b1d1102cbb4211e9bc0146f10117dc3ac86219019753248ff8752f8c4626  MAX_26.24.0_arm64_V20_clone.apk
b236cc87703143ee7f2331bde1cfd5421a26b9c42481e955b0b567b69d19e84d  MAX_26.24.0_arm7_V20.apk
4736f4e052fc27615993e4ae5358978892d1771c97d3027d5ebde90e1f8844d2  MAX_26.24.0_arm7_V20_clone.apk
```

---

Правообладатель оригинального MAX — **ООО «МАХ» (VK)**. Мод не аффилирован, не является официальным продуктом. Использование может нарушать Условия сервиса — **на свой риск**.
