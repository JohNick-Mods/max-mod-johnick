# MAX 26.24.0 RS V8 — Технический аудит мода

**Версия стока**: 26.24.0 (источник — RuStore, RS variant)
**Версия мода**: V8 (Privacy Mod by JohNick)
**Дата**: 2026-07-29
**Совместимость**: `ru.oneme.app` (Android 8.0–16.0 / API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

> **26.24.0 RS V8** — релиз поверх V7. Три новых патча: дедупликация повторных уведомлений распространена на режим «Антиудаление», добавлен дроссель переподключения сервиса, акцентный цвет уведомлений совпадает с выбранным цветом иконки, снят клиентский лимит закреплённых чатов (до 10). **Безопасность и приватность не менялись** — все security-инварианты идентичны V1–V7. Весь функционал V7 сохранён.

---

## 🆕 Что нового в 26.24.0 RS V8

### 1. Дедупликация уведомлений при Антиудалении (`#PUSH-2`)

**Симптом**: при включённом `antiDelete=ON, antiRead=OFF` сервер повторно отдавал пуши «непрочитанное» после реконнекта — пользователь получал повторные уведомления об уже просмотренных сообщениях.

**Шаг A — расширение dedup-гейта**: хелпер `notifShouldSuppress(msgId)` (dedup ring-buffer 500 msgId, введён в V7) раньше активировался только при `antiRead=ON`. Теперь гейт расширен: `antiRead OR antiDelete`. Sentinel `:push2_dedup_gate_v10` в `AppFlags.smali`.

**Шаг B — дроссель `ensureServiceRunning`**: при частых реконнектах (переключение Wi-Fi/мобильной сети) `KeepAliveManager.ensureServiceRunning()` вызывался несколько раз подряд. Добавлен статический throttle 30 сек — если с последнего вызова прошло менее 30 000 мс, метод немедленно возвращает `return-void`. Всё тело обёрнуто в `try/catch Throwable` — при любой ошибке throttle пропускается и `startForegroundService` вызывается штатно. Sentinel `:push2_throttle_v1` в `KeepAliveManager.smali`.

### 2. Цвет уведомления = цвет иконки (`#ICON-1`)

Новый тумблер **«Цвет уведомления = цвет иконки»** (`notifMatchIcon`, по умолчанию ON). При включённом тумблере акцентный цвет уведомления в шторке совпадает с цветом выбранной иконки:

| Цвет иконки | ARGB акцента |
|---|---|
| Синий (0) | стоковый (passthrough `bab.y`) |
| Красный (1) | `0xFFEB3B39` |
| Зелёный (2) | `0xFF4CAF50` |
| Фиолетовый (3) | `0xFF9C27B0` |
| Чёрный (4) | `0xFF424242` (тёмно-серый, различимый в шторке) |
| Оранжевый (5) | `0xFFFF9800` |

Хелпер `AppFlags.notifAccentColor(I)I` вызывается в `c1/nhh.smali` сразу после `iget bab.y` и до `Notification$Builder.setColor(I)`. При `notifMatchIcon=OFF` — passthrough исходного цвета. Sentinel `:icon_notif_color_v1` в `nhh.smali`, `:notif_accent_color_v1` в `AppFlags.smali`.

### 3. Больше закреплённых чатов (до 10) (`#PIN-1`)

Новый тумблер **«Больше закреплённых чатов»** (`unlockPinLimit`, по умолчанию OFF). При включении хелпер `AppFlags.pinLimit(I)I` возвращает `10` вместо серверного значения (обычно `2`). Патч применён к трём call-site'ам `msc.g()I` (функция получения лимита из SDK-конфига):

- `c3/wr0.smali` — multiselect pin (:pin1_callsite_wr0_v1)
- `c3/ta.smali` — одиночный pin (:pin1_callsite_ta_v1)
- `c2/kp2.smali` — третий pin-путь (:pin1_callsite_kp2_v1)

`c1/fg3.smali` (UI-текст «до N чатов») и `c2/me3.smali` (range-query папок) **не тронуты** — проверено регрессионным тестом `test_pin_limit.py`.

⚠️ **Ограничение**: лимит серверный — при синхронизации сервер может сбросить пины сверх 2. Тумблер default=OFF, в description предупреждение.

---

## ⌚ Из V7 — сохранено без изменений

- **🔒 PIN-блокировка запуска** (`appLock`, `appLockUseBiometric`, `changePinAction`).
- **⏩ Скорость воспроизведения** (`playbackSpeed` 0.5×–2×).
- **📞 Гудок на исходящих** (`RingbackTonePlayer` — Samsung + AOSP).
- **🔔 Дедупликация уведомлений при antiRead** (ring-buffer 500 msgId).
- **⌚ Watch-улучшения** (WATCH-11/13/14: nag-баннеры, Stories, кнопка voice).
- **🗑️ Префикс удалённых сообщений**, **🎵 Рингтон MAX на One UI 7**.

---

## 🔧 База V1–V4 (сохранено)

- Антиудаление (4 слоя + офлайн-персист), невидимка, AMOLED, watch-mode, скрытие сервисных чатов.
- 6 цветов иконки (runtime alias-свитч), push клона стабилен, проверка/автопроверка обновлений, дев-меню MAX, SMS-регистрация.

---

## 🛡 Профиль приватности

### Невидимка (`invisible`) — мастер-переключатель

| Дочерний флаг | Что делает |
|---|---|
| Антипечатанье (`antiTyping`) | собеседник не видит набор текста |
| Античиталка (`antiRead`) | собеседник не видит отметку «прочитано» |
| Умная античиталка (`smartAntiRead`) | «прочитано» уходит только ПОСЛЕ вашего ответа |
| Антиприсутствие (`antiPresence`) | онлайн / last-seen скрыты |

### Анти-удаление (`antiDelete`) — 4 слоя + офлайн-персист
1. **L3 — Central WS chat-event**. 2. **L4 — Room ORM body-clear**. 3. **V3 persist**. 4. **Маркер 🗑 + время**. 5. **Офлайн-удаления через FCM** восстанавливаются на старте.

---

## 🔒 Security-инварианты (идентичны V1–V7 — V8 их не касался)

### 🌐 DNS-блокировка телеметрии (P0)
```
trace-flow.ru
sdk-api.apptracer.ru
tracker-api.vk-analytics.ru
tracker.my.com
top-fwz1.mail.ru
```

### 🔕 Приваси-стабы (P0/P1)
- **myTracker (VK)** — нейтрализован (`return-void`).
- **Tracer-инициализаторы (7×)** — застаблены.
- **Firebase Installations** — `getComponents() → emptyList()`.
- **Фоновая выгрузка контактов** — нейтрализована.
- **NFC-HCE** — APDU-ответ `SW 6F00`.
- **TrustManager accept-all** — заменён на `throw`.

### 📱 SMS-регистрация — работает
WS-redirect от сервера MAX **не блокируется** — SMS-код доходит штатно.

### ✍️ Подпись
- **apksigner**, схемы **v2 + v3** (без v1). Cert **SHA-1 `d6a7d757…1298b`** — идентичен для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.

### 📦 Нативные библиотеки
Удаляется **только `libtracernative.so`**. Остальные `.so` — оригинальные из стока 26.24.0.

---

## ⚠ Честно: ограничения в 26.24.0 RS V8

- **#PIN-1 — серверный сброс**: сервер MAX может сбросить пины сверх 2 при синхронизации. Тумблер default=OFF.
- **#ICON-1 — share-меню и «О приложении»**: Samsung Sharesheet и экран App Info берут иконку из `applicationInfo` пакета (OEM-ограничение OneUI), а не из активного alias. Домашний экран и шторка уведомлений — корректны.
- **Ringback (Samsung)**: требует `/system/media/audio/ui/Dialer_new.ogg`.
- **AMOLED — покрытие ~95%**: отдельные bottom-sheets могут остаться тёмно-серыми.
- **Диалоги мода — тёмная схема** (фиксированный тёмный фон).
- **Watch-фичи** активны только при `widthPixels < 500`.
- **Тумблер `improveNotifications` скрыт из UI**.
- **Требуется one-time `pm clear ru.oneme.ap2`** после первой установки клона.
- **Минимальная версия Android — 8.0** (API 26).

---

## ✅ Smoke-тест (2026-07-29)

- Устройство: Samsung S25 (`RFCX5013V5F`), Android 15, arm64.
- Основной (`ru.oneme.app`) + клон (`ru.oneme.ap2`) — **0 FATAL, 0 VerifyError** в логах.
- `install -r` поверх V7 → user-data сохранена, чистый старт.
- Все sentinel'ы V8 присутствуют в собранных DEX: `push2_dedup_gate_v10`, `push2_throttle_v1`, `lastEnsureMs:J`, `notif_accent_color_v1`, `icon_notif_color_v1`, `pin1_callsite_{wr0,ta,kp2}_v1`.
- **#PUSH-2**: повторный пуш при antiDelete=ON не приходит после реконнекта.
- **#ICON-1**: акцент уведомления совпадает с цветом иконки (проверено Red/Green/Purple/Black).
- **#PIN-1**: до 5 закреплённых чатов без сброса сервером (тест на S25).

---

## 🔒 SHA-256 (V8)

```
9d7f5fbe8d6bf3ab7c5c78f45fa65269ccd06d22d811ebc9ea7cb7e284c4592e  MAX_26.24.0_arm64_V8.apk
d28fdf9bf045175739539ee17a03b7488543361169e6fc2c5cee6df697c90fd8  MAX_26.24.0_arm64_V8_clone.apk
8864c375571cc481dfbeb9cac8ebc279a45ff42de78bd4dc36e17f746e8174da  MAX_26.24.0_arm7_V8.apk
0e45a71f20574999f408735450d5b7887dd5bd152a648d0108c747be15e4a0b4  MAX_26.24.0_arm7_V8_clone.apk
```

---

## 📜 Лицензия

Только для личного использования, в учебных и исследовательских целях. Мод предоставляется «as is», подписан debug-ключом, не публикуется в Google Play. Правообладатель оригинального MAX — **ООО «МАХ» (VK)**; мод не аффилирован, не претендует на права на код / знаки / иконки. Использование может нарушать Условия сервиса — **на свой риск**.
