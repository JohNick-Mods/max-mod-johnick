# MAX Mod v26.24.0 V1 (RuStore)

**Сток:** 26.24.0 (источник — RuStore, RS variant) · **Совместимость:** Android 8.0–16.0 (API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

Порт мода под новый сток 26.24.0. Крупный R8-drift относительно 26.23.2 (47 классов пере-отрезолвлены), полностью переписан фильтр списка настроек под новую sealed-архитектуру, мигрированы фильтры чатов/страниц и «При открытии — папка Новые» на новые chokepoint-ы.

## 🆕 Новое в 26.24.0

- **🔧 Порт под сток 26.24.0** — база из RuStore, весь мод пересобран.
- **🎛 `filterSettingsList` — переписан под новую sealed-архитектуру** (`Lxdf;`/`Lwaf;` wrappers + `Lq6h;` title container). Скрытие пунктов настроек снова работает (старый дискриминатор в 26.24.0 давал silent no-op).
- **💬 `filterChatList` / `filterPageList`** — мигрированы на новые типы стока (`Ln13;→Lc33;`, `Les6;→Ltv6;`). Скрытие сервисных чатов и «Только чаты» без регрессий.
- **📁 `startOnNew` — новый chokepoint** `c1/wi3.smali` (`qx6.M(List)` + `r1()Liqi;->h(IZ)V`). «Новые» открываются в основном и в клоне.
- **🛡 `hide_phone` Gate2** — цепочка `b94 → db4 → tc7 → cgf` пере-отрезолвлена.
- **🔎 `manual_update_check` + long-press дев-меню** — экран «О приложении» перепроведён (`kd.onClick :pswitch_42a case 0xc`, ViewHolder `Leq1→Lh1e`, +5 R8-переименований по toolbar-цепочке).
- **🎨 `resources.arsc`** — шаблон под icon-switcher пересобран под 26.24.0 (stable-ids).
- **🔧 R8-rebump: 47 классов** — drift 26.23.2 → 26.24.0 закрыт, полная перекрёстная верификация патчей.

## ⚠ Известные ограничения

- **AMOLED** — покрытие ~95% (список чатов и нижние шторки могут остаться тёмно-серыми).
- **#FILE-1** — PDF/видео/m4a из основного аккаунта временами отправляются с проблемами.

## ✅ Сохранено из 26.23.2 V1

🗑 Иконка корзины + секунды на удалённых (всегда) · Счётчик непрочитанных не накручивается на холодном старте · Цвет иконки (6 цветов) · «Невидимка» мастер-тумблер + порядок отправленных · антиудаление (4 слоя + офлайн-персист, нет push об удалённых, ⏱→✓) · Watch-mode · AMOLED · стабильный push клона · проверка/автопроверка обновлений · дев-меню MAX (без «Дизайн системы») · SMS-регистрация · «Только чаты» и скрытие сервисных чатов/папок/промо · отправка любых файлов.

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.24.0_arm64_V1.apk` | Основной аккаунт, arm64-v8a (большинство современных телефонов). |
| `MAX_26.24.0_arm7_V1.apk` | Основной аккаунт, armeabi-v7a (старые устройства). |
| `MAX_26.24.0_arm64_V1_clone.apk` | Клон arm64. Оранжевая иконка, пакет `ru.oneme.ap2`. |
| `MAX_26.24.0_arm7_V1_clone.apk` | Клон arm7. |

**Клон (первая установка):** после установки — один раз Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS.

## Подпись

- apksigner **v2 + v3** (без v1). Cert SHA-1: `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.
- Smoke: Samsung S25, Android 16 — main + clone без FATAL / VerifyError.

## SHA-256

```
b59b3d02f4215d6b115c78e203b3c732d3d23979276693ad5ab76bc83629d87b  MAX_26.24.0_arm64_V1.apk
e879ef9c6765dfc9a3f90a11155dc289b68813bb2db0dc7a74f0c5cb78c43f61  MAX_26.24.0_arm64_V1_clone.apk
b646898cab055ccad1ebf9cc7831af0798b2affa6335854b7f954104f66c9d89  MAX_26.24.0_arm7_V1.apk
99f625378e8a230384b8fc983d8db9f4cc5ca9fd3b4ba80ea5c83706e6d45e46  MAX_26.24.0_arm7_V1_clone.apk
```

---

Правообладатель оригинального MAX — **ООО «МАХ» (VK)**. Мод не аффилирован, не является официальным продуктом. Использование может нарушать Условия сервиса — **на свой риск**.
