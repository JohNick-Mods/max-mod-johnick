# MAX Mod v26.24.0 V10 (RuStore)

**Сток:** 26.24.0 (RuStore, RS variant) · **Совместимость:** Android 8.0–16.0 (API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

Двойной хотфикс поверх V8. **Весь функционал V8 сохранён, безопасность и приватность не менялись.** Ставится через `install -r` поверх любой сборки 26.x — данные сохраняются.

## 🆕 Что нового в V10 (относительно V8)

### 📦 Оптимизация размера APK (V9)
Уровень сжатия при репаке поднят до максимального (compresslevel 9). Размер APK уменьшен на **−17.6%** (arm64: ~40 МБ → ~33 МБ). Совместимость и производительность не изменились.

### 📞 Исправление непрерывного гудка дозвона (V10, #USER-2 V29)
В V8 и ранее гудок на исходящем обрывался раньше реального ответа собеседника. Корневая причина: `stop()` вызывался в `fc1.b()` (markActive), но Android Telecom закрывает audio session с задержкой ~2 сек — в этот промежуток слышалась тишина вместо разговора.

Исправление: `stop()` перенесён в `d25.onCallAccepted()` — точку, когда собеседник реально поднял трубку. Реализация плеера упрощена до атомарного `ToneGenerator(STREAM_VOICE_CALL, 80).startTone(TONE_SUP_RINGTONE=23, -1)` — единый путь для всех устройств.

---

## 📦 Из V8 — сохранено

- **🔔 Фикс повторных уведомлений при Антиудалении** (#PUSH-2): dedup-гейт `antiRead OR antiDelete` + throttle 30 сек.
- **🎨 Цвет уведомления = цвет иконки** (#ICON-1): тумблер `notifMatchIcon` (ON по умолчанию).
- **📌 Больше закреплённых чатов** (#PIN-1): тумблер `unlockPinLimit` (OFF по умолчанию, лимит до 10).
- PIN-блокировка, скорость воспроизведения, watch-фичи, невидимка, антиудаление, AMOLED, 6 цветов иконки, проверка обновлений, дев-меню, SMS-регистрация.

## 📱 SMS-регистрация — работает

WS-redirect от сервера MAX не блокируется. SMS-код доходит штатно.

## ⚠ Известные ограничения

- **Ringback**: ToneGenerator маршрутизируется через STREAM_VOICE_CALL; на устройствах без активной VoIP-сессии в момент DIALING гудок может быть тихим.
- **#PIN-1**: сервер может сбросить пины >2 при синхронизации.
- **AMOLED** — покрытие ~95%.
- **Watch-фичи** активны только при `widthPixels < 500`.

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.24.0_arm64_V10.apk` | Основной аккаунт, arm64-v8a (большинство современных телефонов). |
| `MAX_26.24.0_arm7_V10.apk` | Основной аккаунт, armeabi-v7a (старые устройства). |
| `MAX_26.24.0_arm64_V10_clone.apk` | Клон arm64. Оранжевая иконка, пакет `ru.oneme.ap2`. |
| `MAX_26.24.0_arm7_V10_clone.apk` | Клон arm7 (для WearOS / OnePlus Watch 3). |

**Клон (первая установка):** после установки — один раз Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS.

## Подпись

- apksigner **v2 + v3** (без v1). Cert SHA-1: `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.
- Smoke: Samsung S25 (`RFCX5013V5F`), Android 15, arm64 — main + clone без FATAL / VerifyError.

## SHA-256

```
6f2584309d214deb7c2171bc97209cfe44a9506e9e21a82e9c4d10b760f13082  MAX_26.24.0_arm64_V10.apk
774d69f04734c31e38081694720aa69b2c16bf5d24018ed52f037bacada56930  MAX_26.24.0_arm64_V10_clone.apk
f3132e2c54bcf747db591157784fe33888bfaf757b4f104908ce0b6afd157720  MAX_26.24.0_arm7_V10.apk
8488dab36240e97f587767a8c3496fb36bcddf6328db0b5caf03eb1070b7b2bd  MAX_26.24.0_arm7_V10_clone.apk
```

---

Правообладатель оригинального MAX — **ООО «МАХ» (VK)**. Мод не аффилирован, не является официальным продуктом. Использование может нарушать Условия сервиса — **на свой риск**.
