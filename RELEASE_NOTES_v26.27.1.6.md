# MAX Mod v26.27.1 — V6 (2026-08-18)

## Новое

- **🖼 Обои чата.** Выбранная картинка показывается фоном и в списке чатов
  («Чаты»), и внутри каждого чата. Выбор — «Настройки мода → 🎨 Вид →
  🖼 Выбрать обои чата». Сброс — «🧹 Сбросить обои». Реализация через
  `View.setBackground(BitmapDrawable)` — не ломает layout, скролл списка
  работает нормально.

- **👻 Тумблер «Прозрачная тема».** Переключатель показа обоев в
  категории ВИД: **ON** — картинка видна, **OFF** — обои скрыты
  (файл сохраняется, можно быстро включить обратно).

- **🌞🌙⚫ Переключатель темы (Свет / Тёмная / AMOLED).** Кнопки в
  категории ВИД теперь **реально меняют тему на главных экранах**
  (чат-лист, чаты, звонки, настройки), а не только внутри диалога мода.
  Активная тема подсвечена зелёным. Работает через нативный toggle-механизм
  MAX (`kp3.q`) — синхронизируется с in-memory cache и reactive-стримом,
  как штатный переключатель.

- **🎛 Цвет тумблеров.** OFF заметно отличается от ON: серый (#757575) vs
  зелёный акцент. Стало сразу понятно, что включено, а что нет.

## Сохранено с V5

- Авто-rotate (USER orientation).
- Лимит закреплённых чатов снят по умолчанию.
- Приватность PIN (`hideView` + `bgWatchdog`).
- Drag: `ACTION_DOWN` для persist chatPin.

## Установка

- Первая установка / чистая: любой `.apk`.
- Обновление V5 → V6: `install -r` — данные сохранятся.
- С другой подписи (донор и т.п.): сначала `uninstall`.

## Файлы

- `MAX_26.27.1_arm64_V6.apk` — main (arm64-v8a)
- `MAX_26.27.1_arm64_V6_clone.apk` — clone (arm64-v8a)
- `MAX_26.27.1_arm7_V6.apk` — main (armeabi-v7a)
- `MAX_26.27.1_arm7_V6_clone.apk` — clone (armeabi-v7a)
- `SHA256SUMS` — контрольные суммы
- `AUDIT_26.27.1_V6.pdf` — аудит патчей
