# MAX Mod v26.24.0 V2 (RuStore) — UI-рефактор диалогов

**Сток:** 26.24.0 (RuStore, RS variant) · **Совместимость:** Android 8.0–16.0 (API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

UI-обновление поверх V1: закруглённые диалоги, нативные радиокнопки выбора цвета иконки, переписанный экран настроек мода. **Функционал V1 полностью сохранён, безопасность/приватность не менялись.** Ставится через `install -r` поверх V1 (или любой сборки 26.x) — данные сохраняются.

## 🆕 Новое в V2 — UI-рефактор диалогов

- **🔵 Закруглённые диалоги.** Все диалоги мода (меню настроек, категории, дев-меню, «Проверить обновления», разрешение ссылок max.ru) — закруглённые углы 16dp, единый современный вид. Фон следует системной теме.
- **🎨 Выбор цвета иконки — нативные радиокнопки.** Вместо текстовых ◉/○ — настоящие Android RadioButton (6 цветов). Выбор нагляднее, применяется на лету, переживает перезапуск.
- **🧩 Новый класс `DialogsUtils`.** Тема-детект и показ диалогов вынесены в единый util-класс (раньше дублировалось в 6 местах).
- **📐 Экран настроек мода переписан (−63% кода).** Хелперы `addSwitchRow` / `showDialog`, исправлена разметка диалогов. Все тумблеры, категории и каскад «Невидимки» сохранены.
- **🌗 Тема диалогов — надёжно по системной теме.** Определение тёмной/светлой темы переведено на системный night-mode (как AMOLED) — R8-независимо, не ломается при обновлении стока.

Порт из донор-сборки MAX_26.24.0.64.

## ⚠ Известные ограничения (те же, что в V1)

- **AMOLED** — покрытие ~95% (список чатов и нижние шторки могут остаться тёмно-серыми).
- **#FILE-1** — PDF/видео/m4a из основного аккаунта временами отправляются с проблемами.
- **Закруглённые диалоги** — best-effort: у отдельных системных диалогов (не AlertDialog мода) углы могут остаться прямыми.

## ✅ Что было в V1 — всё на месте

Порт под сток 26.24.0 (RuStore), R8-rebump 47 классов, `filterSettingsList` под новую sealed-архитектуру, миграция фильтров чатов/страниц, `startOnNew` на новом chokepoint, `hide_phone` Gate2, `manual_update_check` + long-press дев-меню, icon-switcher. Антиудаление (4 слоя + офлайн-персист), «Невидимка», Watch-mode, AMOLED, push клона, проверка обновлений, SMS-регистрация, скрытие сервисных чатов/папок/промо, отправка любых файлов.

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.24.0_arm64_V2.apk` | Основной аккаунт, arm64-v8a (большинство современных телефонов). |
| `MAX_26.24.0_arm7_V2.apk` | Основной аккаунт, armeabi-v7a (старые устройства). |
| `MAX_26.24.0_arm64_V2_clone.apk` | Клон arm64. Оранжевая иконка, пакет `ru.oneme.ap2`. |
| `MAX_26.24.0_arm7_V2_clone.apk` | Клон arm7. |

**Клон (первая установка):** после установки — один раз Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS.

## Подпись

- apksigner **v2 + v3** (без v1). Cert SHA-1: `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.
- Smoke: Samsung S25, Android 16 — main + clone без FATAL / VerifyError, ART verify PERFORMED.

## SHA-256

```
c9e452bd45b5729e43efd124dcdc6da0fd2338d023bbd73b3094e4ad4b38873c  MAX_26.24.0_arm64_V2.apk
497330c3588065b8a75a3a2c96c955e070bbb1cceab974e7d723f621a36a42c1  MAX_26.24.0_arm64_V2_clone.apk
77c192757085931bd5d2e83df9b7f510ce00aa61170e7e85b2369eefa276bbd4  MAX_26.24.0_arm7_V2.apk
dd7d3b2c46198ec724c47ce8cbf40f5d9426a75330bb4f1e538a054a8206cb91  MAX_26.24.0_arm7_V2_clone.apk
```

---

Правообладатель оригинального MAX — **ООО «МАХ» (VK)**. Мод не аффилирован, не является официальным продуктом. Использование может нарушать Условия сервиса — **на свой риск**.
