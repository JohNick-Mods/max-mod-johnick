# MAX 26.26.0 V5 — Технический аудит мода

**Версия стока**: 26.26.0  
**Версия мода**: V5 (Privacy Mod by JohNick)  
**Дата**: 2026-08-13  
**Совместимость**: `ru.oneme.app` (Android 8.0–16.0 / API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

> **V5**: визуальная метка удалённых сообщений (красный ❌ + затемнение пузырька, antiDelete-gated); фикс прокрутки чата к последним сообщениям при повторном заходе; уточнение формулировки про цвет иконки уведомления (системное ограничение ОС).
> Изменения V5 — **UX-only**: слой нейтрализации телеметрии и приватности идентичен V4 (см. разделы ниже без изменений).

---

## 🆕 V5 — визуальная метка удалённых + фиксы

### ❌ Визуальная метка удалённых сообщений (антиудаление)
- Удалённое собеседником, но сохранённое модом сообщение помечается красным крестиком **❌** перед текстом и **затемняется** (setAlpha 0.5f на пузырьке).
- Реализация:
  - Префикс: `AppFlags.applyTrashPrefix()` (❌-глиф, идемпотентен) в мапперах `c1/ome.smali` (Room cold-read) и `c3/sel.smali` (get-by-id), antiDelete-gated.
  - Затемнение: `c2/laa.smali::Q()` → `View.setAlpha(0.5f)` по `AppFlags.isDeletedKept(msgId)`, antiDelete-gated (reset 1.0f обязателен — VH переиспользуется).
  - Заполнение `dk_ids`: `c3/sel.smali` вызывает `addDeletedKept` перед `applyTrashPrefix`; bulk WS-путь `c3/rfb.smali::b` помечает массив id + эмитит событие в шину `La51` (sentinel `:ad_l3_refresh_v4`).
- Известное ограничение: самый свежий удалённый элемент (при открытом чате) получает метку не мгновенно, а при следующем событии списка. Данные сохраняются корректно.

### 📜 Фикс: чат открывался на старых прочитанных сообщениях
- Причина: накопленное в фоне scroll-событие (`Lara;->c()`) не потреблялось при повторном показе уже открытого чата — оно вызывается только из 4 caller'ов (adapter-update / Flow-emit / onViewCreated), ни один не срабатывает на повторный attach.
- Фикс: форс-консьюм `Lara;->c()` в `MessagesListWidget.onAttach` (единственный lifecycle-callback на каждый повторный показ), гейт `smartAntiRead || antiRead` (sentinel `:sar_onattach_v4`).

### 📝 Уточнение: цвет иконки уведомления
- `apply_v_icon_notif_color.py` (Notification.Builder.setColor) меняет только акцент/фон в **развёрнутом** уведомлении. Маленькую иконку в статус-баре Android рисует монохромной у всех приложений с версии 5.0 — системное ограничение ОС, не баг мода. Формулировка в README/GH уточнена.

---

## 🆕 V4 — UX-фичи (унаследовано)

- **Ручной порядок закреплённых чатов** (`apply_v_pinned_sort.py` + `apply_v_pinned_drag.py`, drag за pin-иконку, локально, безусловно).
- **Авто-выход из групп для любых чатов** (тумблер `autoLeaveGroupInvites`, enqueue `ome.i` + drain `ou2.W`).
- Фиксы: SMS-Retriever инициализация вне гейта; `hideChannels` вынесен из общего OR-гейта filterChatList; сужены substring-фильтры сервис-чатов; smartAntiRead scroll-fix v3 (ветка invisible=OFF).

---

## 🐞 Исправлено в V3 (унаследовано)

- Убрана иконка 🗑️ на удалённых сообщениях (`:ad_trash_v1`/`:ad_trash_ws_v1` исключены). В V5 маркер вернулся как ❌ по явному запросу — идемпотентный `applyTrashPrefix`.

---

## 🔍 Верификация (V5)

| Проверка | Результат |
|---|---|
| Reducer-хирургия `qma.smali` (краш-фикс) откачена | **0 вхождений** `ad_reducer_rebuild`/`adviz_probe` |
| VerifyError `qma` на устройстве (открытие чата) | **отсутствует** (crash-буфер пуст) |
| sentinel `:ad_l3_refresh_v4` (bulk-refresh) | ✅ присутствует |
| sentinel `:sar_onattach_v4` (scroll-fix) | ✅ присутствует |
| ❌-глиф в `applyTrashPrefix` | ✅ присутствует |
| `apksigner verify` (все 4 APK) | ✅ v2+v3 |
| exact ABI (arm64/arm7), no tracer | ✅ |
| distinct SHA arm64 ≠ arm7 | ✅ |

**Smoke test V5:** S25 Ultra (RFCX5013V5F) — `install -r`, 0 FATAL/VerifyError, чат открывается без краша. ❌-метка + затемнение отображаются на удалённых сообщениях (кроме мгновенного показа самого свежего — self-heal при следующем действии).

---

## 🛡 Профиль приватности (без изменений с V1)

### Невидимка (`invisible`) — мастер-переключатель

| Дочерний флаг | Что делает |
|---|---|
| `antiTyping` | собеседник не видит набор текста |
| `antiRead` | собеседник не видит отметку «прочитано» |
| `smartAntiRead` | «прочитано» уходит только после вашего ответа (consume-on-use) |
| `antiPresence` | онлайн / last-seen скрыты |

### Антиудаление (`antiDelete`) — 4 слоя

1. **L3** — Central WS chat-event (live-канал).
2. **L4** — Room ORM body-clear (локальный SQL-сброс).
3. **Маркер** ❌ + «· удалено» + время удаления (ЧЧ:ММ:СС ДД.ММ), antiDelete-gated.
4. **FCM офлайн-персист** — удаления при убитом приложении восстанавливаются при следующем старте.

### Нейтрализация телеметрии — 4 чокпоинта (V1)

`p0_telem_push`, `p0_telem_onelog`, `p0_telem_operator` (IP-strip), `p0_telem_dps`.

---

## 📦 Артефакты (V5)

| Файл | SHA-256 |
|---|---|
| `MAX_26.26.0_arm64_V5.apk` | `241e421a9966b29a54cad4c686f7964e16b9143e6c7e55b651a960b1f75b8cdf` |
| `MAX_26.26.0_arm64_V5_clone.apk` | `890f86dda938039bc06f3cd45b77c65ae691bab3a34beac160b98dcbea90434b` |
| `MAX_26.26.0_arm7_V5.apk` | `5ee7e53a307b47c42395b68f771bb87459863749eb678d298c782cabd88dc1a3` |
| `MAX_26.26.0_arm7_V5_clone.apk` | `62d5ae136412122b43fb73f159e92bde5d885060c848fffbf4081367a1177282` |

Подпись: v2+v3 APK Signature Scheme. Cert SHA-1 идентичен V1–V4 — install -r без потери данных.

---

*Аудит выполнен статическим анализом smali (baksmali + независимый агент-верификатор) и рантайм-логами (logcat-пробники). Динамическое тестирование: smoke на S25 Ultra.*
