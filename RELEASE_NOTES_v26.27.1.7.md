# MAX Mod v26.27.1 — V7 (2026-08-20)

## Новое

- **💬 Отдельный тумблер «Прозрачный фон в чатах».** Теперь обои можно
  показывать **только внутри открытого чата**, оставив список чатов, звонки
  и настройки на обычном тёмном фоне. Новый переключатель в категории
  «🎨 ВИД» работает **независимо** от общей «Прозрачной темы»:
  - **ВИД → 💬 Прозрачный фон в чатах = ON, Прозрачная тема = OFF** —
    список тёмный, обои видны только когда заходишь в чат.
  - Оба ON — обои и на списке, и в чатах (как раньше).
  Реализовано динамически (фон общего контейнера списка/чата переключается
  по открытию/закрытию чата), поэтому список не «просвечивает».

- **📷 Камера/галерея в альбомном режиме — фикс.** После возврата
  авто-поворота (V5) пикатель вложений («Фото и видео» + Галерея / Файл /
  Опрос / Место / Контакт) в горизонтальной ориентации разъезжался.
  Теперь при открытии скрепки в альбомном режиме пикатель принудительно
  открывается в портрете и рендерится корректно, при закрытии ориентация
  возвращается. Проверено на S25.

## Сохранено с V6

- 🖼 Обои чата на списке чатов (`View.setBackground(BitmapDrawable)`).
- 🎨 Тумблер «Прозрачная тема» как независимый гейт обоев списка.
- 🌞🌙⚫ Переключатель тем «Свет / Тёмная / AMOLED» на всех главных экранах.
- 🎛 Цвет тумблеров: серый OFF / зелёный ON.

## Сохранено с V5

- Авто-rotate (USER orientation).
- Лимит закреплённых чатов снят по умолчанию.
- Приватность PIN (`hideView` + `bgWatchdog`).
- Drag: `ACTION_DOWN` для persist chatPin.

## Установка

- Первая установка / чистая: любой `.apk`.
- Обновление V6 → V7: `install -r` — данные сохранятся.
- С другой подписи (донор и т.п.): сначала `uninstall`.

## Файлы

- `MAX_26.27.1_arm64_V7.apk` — main (arm64-v8a)
- `MAX_26.27.1_arm64_V7_clone.apk` — clone (arm64-v8a)
- `MAX_26.27.1_arm7_V7.apk` — main (armeabi-v7a)
- `MAX_26.27.1_arm7_V7_clone.apk` — clone (armeabi-v7a)
- `SHA256SUMS` — контрольные суммы
- `AUDIT_26.27.1_V7.pdf` — аудит патчей
