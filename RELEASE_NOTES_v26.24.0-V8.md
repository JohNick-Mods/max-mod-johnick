# MAX Mod v26.24.0 V8 (RuStore)

**Сток:** 26.24.0 (RuStore, RS variant) · **Совместимость:** Android 8.0–16.0 (API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

Релиз поверх V7. **Весь функционал V7 сохранён, безопасность и приватность не менялись.** Ставится через `install -r` поверх любой сборки 26.x — данные сохраняются.

## 🆕 Что нового в V8

### 🔔 Фикс повторных уведомлений при Антиудалении (#PUSH-2)
При `antiDelete=ON` сервер мог повторно присылать пуши о прочитанных сообщениях после реконнекта. Теперь dedup-гейт (`notifShouldSuppress`, ring-buffer 500 msgId) активен и при `antiDelete=ON`, и при `antiRead=ON`. Также добавлен throttle 30 сек на переподключение WS-сервиса — устраняет частые повторные старты при переключении сети.

### 🎨 Цвет уведомления = цвет иконки (#ICON-1)
Новый тумблер **«Цвет уведомления = цвет иконки»** (по умолчанию ON). Акцентный цвет в шторке уведомлений совпадает с выбранным цветом иконки: Красный → `#EB3B39`, Зелёный → `#4CAF50`, Фиолетовый → `#9C27B0`, Чёрный → `#424242` (тёмно-серый, различимый), Оранжевый → `#FF9800`. Синий — стоковый цвет. При выключенном тумблере — цвет как в стоке.

### 📌 Больше закреплённых чатов — до 10 (#PIN-1)
Новый тумблер **«Больше закреплённых чатов»** (по умолчанию OFF). При включении лимит на стороне клиента поднимается до 10. ⚠️ Лимит серверный: при синхронизации сервер может сбросить пины сверх 2. Тумблер выключен по умолчанию.

---

## ⌚ Из V7 — сохранено

- PIN-блокировка запуска + биометрия + смена PIN.
- Скорость воспроизведения голосовых и кружков (0.5×–2×).
- Гудок на исходящих звонках (Samsung + AOSP).
- Watch-улучшения (WATCH-11/13/14).
- Все V1–V4 патчи: антиудаление, невидимка, AMOLED, 6 цветов иконки, push клона, проверка обновлений, дев-меню, SMS-регистрация.

## 📱 SMS-регистрация — работает

WS-redirect от сервера MAX не блокируется. SMS-код доходит штатно.

## ⚠ Известные ограничения

- **#PIN-1**: сервер может сбросить пины >2 при синхронизации.
- **Иконка в share-меню и «О приложении»**: Samsung Sharesheet/AppInfo берут иконку пакета (OEM-ограничение OneUI) — домашний экран и шторка работают корректно.
- **AMOLED** — покрытие ~95%.
- **Watch-фичи** активны только при `widthPixels < 500`.

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.24.0_arm64_V8.apk` | Основной аккаунт, arm64-v8a (большинство современных телефонов). |
| `MAX_26.24.0_arm7_V8.apk` | Основной аккаунт, armeabi-v7a (старые устройства). |
| `MAX_26.24.0_arm64_V8_clone.apk` | Клон arm64. Оранжевая иконка, пакет `ru.oneme.ap2`. |
| `MAX_26.24.0_arm7_V8_clone.apk` | Клон arm7 (для WearOS / OnePlus Watch 3). |

**Клон (первая установка):** после установки — один раз Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS.

## Подпись

- apksigner **v2 + v3** (без v1). Cert SHA-1: `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.
- Smoke: Samsung S25 (`RFCX5013V5F`), Android 15, arm64 — main + clone без FATAL / VerifyError.

## SHA-256

```
9d7f5fbe8d6bf3ab7c5c78f45fa65269ccd06d22d811ebc9ea7cb7e284c4592e  MAX_26.24.0_arm64_V8.apk
d28fdf9bf045175739539ee17a03b7488543361169e6fc2c5cee6df697c90fd8  MAX_26.24.0_arm64_V8_clone.apk
8864c375571cc481dfbeb9cac8ebc279a45ff42de78bd4dc36e17f746e8174da  MAX_26.24.0_arm7_V8.apk
0e45a71f20574999f408735450d5b7887dd5bd152a648d0108c747be15e4a0b4  MAX_26.24.0_arm7_V8_clone.apk
```

---

Правообладатель оригинального MAX — **ООО «МАХ» (VK)**. Мод не аффилирован, не является официальным продуктом. Использование может нарушать Условия сервиса — **на свой риск**.
