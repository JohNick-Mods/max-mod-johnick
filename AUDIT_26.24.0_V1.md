# MAX 26.24.0 RS V1 — Технический аудит мода

**Версия стока**: 26.24.0 (источник — RuStore, RS variant)
**Версия мода**: V1 (Privacy Mod by JohNick)
**Дата**: 2026-07-24
**Совместимость**: `ru.oneme.app` (Android 8.0–16.0 / API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

> **26.24.0 RS V1** — порт мода под новый сток 26.24.0 из RuStore. Крупный R8-drift относительно 26.23.2 (47 классов пере-отрезолвлены). Полностью переписан `filterSettingsList` под новую sealed-архитектуру списка настроек. Мигрированы фильтры чатов/папок и «При открытии — папка Новые» на новые chokepoint-ы. Обновлён шаблон `resources.arsc` под icon-switcher stable-ids.

---

## 🆕 Новое в 26.24.0 RS V1

### 🔧 Портирование стока 26.24.0
Базовый источник — сток MAX 26.24.0 из RuStore (RS variant). Anti-split → сборка мода поверх стокового дерева 26.24.0.

### 🔧 R8-rebump: 47 классов пере-отрезолвлены
Drift 26.23.2 → 26.24.0 закрыт: `fingerprint_resolver --check-r8-drift` прогнан по всей карте, обновлён `r8_mapping.py` (новый dataclass, `LATEST=26.24.0`). Перекрёстная верификация всех apply-скриптов против собранного дерева.

### 🎛 `filterSettingsList` — переписан под новую sealed-архитектуру
В 26.24.0 список настроек переработан на sealed-иерархию: элементы обёрнуты в `Lxdf;` / `Lwaf;` wrappers, заголовки категорий — в `Lq6h;` title container с подтипами. Старый дискриминатор фильтра (26.23.2) в 26.24.0 приводил к silent no-op. Фильтр полностью переписан под новые типы.

### 📁 `filterPageList` — миграция `Les6;` → `Ltv6;`
Дискриминатор фильтра страниц (используется в скрытии сервисных чатов) мигрирован на новый R8-тип 26.24.0.

### 💬 `filterChatList` — миграция `Ln13;` → `Lc33;`
Фильтр списка чатов (скрытие сервисных диалогов, «Только чаты») переведён на новый тип-контейнер элемента списка.

### 📁 `startOnNew` — новый chokepoint `c1/wi3.smali`
«При открытии — папка Новые» мигрирован на новый chokepoint 26.24.0: `qx6.M(List)` + вызов `r1()Liqi;->h(IZ)V`. Работает и в основном приложении, и в клоне.

### 🛡 `hide_phone` Gate2 — обновлён под R8-drift
Цепочка Gate2 в скрытии номера пере-отрезолвлена: `b94 → db4 → tc7 → cgf` (все четыре класса сменили имена в 26.24.0).

### 🔎 `manual_update_check` — re-enabled под 26.24.0
Пункт «Проверить обновления» пере-подключён к новой раскладке ViewHolder-а `About`-экрана: `kd.onClick :pswitch_42a case 0xc`, ViewHolder `Leq1` → `Lh1e`. Short-tap по версии снова открывает диалог проверки.

### 🎛 Toolbar «О приложении» — long-press работает
Long-press по строке версии восстановлен через `setClickable+setLongClickable` на нужном View + 5 R8-renames в цепочке ToolbarActivity/ViewHolder-ов: `Lqub → Ls0c`, `Lwtb → Lyzb`, `Lusd → Lj0e`, `Ls21 → Lm31`, `Lepb → Livb`.

### 🎨 `resources.arsc` — обновлённый шаблон
Шаблон `resources.arsc` пересобран под 26.24.0 (icon-switcher stable-ids через `aapt2 --stable-ids`) — цвет иконки продолжает меняться на лету без переустановки.

---

## 🐞 Сохранено из 26.23.2 V1 (порт под сток 26.24.0)

- 🗑 **Секунды и иконка корзины на удалённых сообщениях** — безусловно на рендере (`ЧЧ:ММ:СС ДД.ММ` + 🗑).
- 🔢 **Счётчик непрочитанных** не накручивается на холодном старте.
- 🎛 **Dev-меню** без нерабочего пункта «Дизайн система».
- 🎨 **«Цвет иконки» — 6 цветов** (синий/красный/зелёный/фиолетовый/чёрный/оранжевый), меняется на лету без переустановки.
- 📤 **Порядок отправленных при «Невидимке»** — пересортировка на входе адаптера (gated `invisible=ON`).
- 🕹 **«Невидимка» — единый мастер-тумблер** каскадно управляет `antiRead`/`antiTyping`/`antiPresence`.
- 🛡 **Крашфиксы** при запуске (`startOnNew`) и при открытии вкладки «Чаты» (`Resources$NotFoundException`).
- ⌚ **Watch-mode** — авто-адаптация плотности под маленькие экраны (< 500 px), без тумблера.
- 🌑 **AMOLED-тема** — чисто чёрный `#000000` фон, авто при системной тёмной теме.
- 📲 **Push у клона стабилен** — клон генерирует свой `device_id` один раз.
- 🔕 **Нет push об удалённых** при `antiDelete=ON` (гейт в FCM-обработчике).
- ⏱→✓ **Часы на исходящих** при `antiDelete=ON` — статус `SENDING → SENT`.
- 🔎 **Проверка обновлений** (short-tap по версии) + **автопроверка opt-in**.
- 🎛 **Дев-меню MAX** (long-press по версии).
- 📱 **SMS-регистрация** — User-Agent 26.24.0, код при регистрации приходит.

---

## 🛡 Профиль приватности

### Невидимка (`invisible`) — мастер-переключатель
Взводит дочерние приватные флаги; каждый работает и отдельно.

| Дочерний флаг | Что делает |
|---|---|
| Антипечатанье (`antiTyping`) | собеседник не видит набор текста |
| Античиталка (`antiRead`) | собеседник не видит отметку «прочитано» |
| Умная античиталка (`smartAntiRead`) | «прочитано» уходит только ПОСЛЕ вашего ответа |
| Антиприсутствие (`antiPresence`) | онлайн / last-seen скрыты (сервер видит offline) — независимый тумблер |

**Взаимоисключение**: `antiRead` и `smartAntiRead` не могут быть включены одновременно. Оба блокируют внутричатовый счётчик «N новых» и скроллят в конец при открытии.

### Анти-удаление (`antiDelete`) — 4 слоя + офлайн-персист
1. **L3 — Central WS chat-event**: собеседник удаляет при онлайн-приложении.
2. **L4 — Room ORM body-clear**: локальный SQL-сброс тела блокируется.
3. **V3 persist**: удалённые сообщения остаются видимыми после ре-синка.
4. **Маркер 🗑 + время `ЧЧ:ММ:СС ДД.ММ`** в строке времени (безусловно на рендере).
5. **Офлайн-удаления через FCM** — метка восстанавливается на следующем старте, если удаление пришло при убитом OEM-killer'ом приложении.

Восстановленные удалённые сохраняются с `isRead=1` — не задирают счётчик непрочитанных.

### 📢 Каналы = сток при включённых античиталках
При `antiRead`/`smartAntiRead`/`invisible` read-mark для каналов всегда уходит на сервер (сток-поведение).

---

## 📋 Переключатели в UI

Все по умолчанию **OFF** (кроме `amoledTheme` — авто при тёмной теме) — до первого включения мод ведёт себя идентично стоку (антидетект). Диалог настроек следует системной теме. Открытие: **Настройки → О приложении → long-press по строке версии**.

**Приватность (10):** `antiDelete`, `invisible` (+ `antiTyping` / `antiRead` / `smartAntiRead` / `antiPresence` — независим), `blockMobileIdVerify`, `hidePhoneNumber`, `hideDigitalId`, `gateOpcode5`.

**Папки и вкладки (7):** `hideAll`, `hideSferum`, `hideFavorites`, `hideBusinessPromo`, `hideInviteFriends`, `reorderTabs`, `startOnNew`.

**Сервисные чаты (9):** `hideVerifyCodes`, `hideMaxOfficial`, `hideInteresting`, `hideUsefulNotifs`, `hideMaxBusiness`, `hidePromoWidgets`, `hideInviteLinks`, `hideChannels` («Только чаты»), `hideChannelRecs`.

**Звонки и система (5):** `autoStart`, `backCameraForNote`, `debugMenu`, `gateOpcode5`, `amoledTheme`.

**Внешний вид (1):** `iconColor` (6 цветов, radio).

> Автопроверка обновлений управляется отдельным чекбоксом в диалоге проверки. Watch-mode активируется автоматически по ширине экрана — тумблера нет. Тумблер `improveNotifications` присутствует в бинарнике для совместимости, но **скрыт из UI**.

---

## 🔒 Security-инварианты

### 🌐 DNS-блокировка телеметрии (P0)
```
trace-flow.ru
sdk-api.apptracer.ru
tracker-api.vk-analytics.ru
tracker.my.com
top-fwz1.mail.ru
```

### 🔕 Приваси-стабы (P0/P1)
- **myTracker (VK)** — вызовы SDK нейтрализованы (`return-void`).
- **Tracer-инициализаторы (7×)** — застаблены (`EMPTY_LIST` / `Object()`), SDK не стартует.
- **Firebase Installations** — `getComponents() → emptyList()` (нет HTTP к googleapis).
- **Фоновая выгрузка контактов** — метод нейтрализован (`return-void`).
- **NFC-HCE** — APDU-ответ `SW 6F00` (сервис не обслуживается).
- **TrustManager accept-all** — доверие «любому» сертификату заменено на `throw`.
- **No-IP телеметрия** — IP-адрес не отправляется в телеметрию соединения.

### ✍️ Подпись
- **apksigner**, схемы **v2 + v3** (без v1 / JAR-signing).
- Ключ: `debug.jks` (стандарт Mods by JohNick), cert **SHA-1 `d6a7d757…1298b`** — идентичен для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.

### 📦 Нативные библиотеки
Удаляется **только `libtracernative.so`**. Остальные `.so` — оригинальные из стока 26.24.0.

---

## 🔓 Always-on (без тумблера)

- **🗑 + секунды на удалённых** — маркер удалённого сообщения на рендере.
- **Watch-mode** (авто-детект ширины < 500 px → override плотности).
- Отправка любых файлов (обход клиентского блэклиста + маскировка `.bin` для сервера).
- Изоляция device_id клона, dynamic FileProvider authority для клона.
- Подавление ложного красного «!» при Невидимке.
- SMS-авторизация при регистрации (User-Agent 26.24.0).
- Скриншоты и запись экрана разрешены везде (`FLAG_SECURE` снят).
- IP-адрес не отправляется в телеметрию соединения.
- Автоповорот следует настройке устройства.
- Диалог «Разрешить ссылки max.ru» (one-guard) — App Links для Цифрового ID.
- TrustManager accept-all → отклоняется; logcat-dump → выключен; фоновая выгрузка контактов → выключена; NFC-HCE → SW 6F00; myTracker (VK) → выключен.
- Антиудаление (4 слоя + офлайн-персист), tracer-stubs, AppFlags init, KeepAlive, dev-меню MAX по long-press.

---

## ⚠ Честно: чего НЕТ / ограничения в 26.24.0 RS V1

- **AMOLED — покрытие ~95%**: список чатов, окно чата и bottom-sheets могут остаться тёмно-серыми (рисуют собственный фон).
- **`#FILE-1`**: PDF / видео / m4a из основного аккаунта временами отправляются с проблемами.
- **Тумблер `improveNotifications` скрыт из UI** (жёсткие проверки Android 15 / OxygenOS 15).
- **«Цифровой ID» и «Госуслуги баннер»** контролируются одним `hideDigitalId` (общий серверный ключ PmsProperty).
- **Требуется one-time `pm clear ru.oneme.ap2`** после установки APK клона.
- **Минимальная версия Android — 8.0** (API 26) — ограничение стока.

---

## 🔐 Безопасность и сборка

- **Подпись**: apksigner v2 + v3 (debug.jks), cert SHA-1 `d6a7d757…1298b`.
- **DEX**: 3 шт. (c1/c2/c3), сторонние библиотеки — оригинальные из стока 26.24.0.
- **Нативные либы**: удаляется только `libtracernative.so`.
- **Телеметрия**: только дефолтная MAX, DNS-блок P0-доменов трекинга.
- **Источник**: сток 26.24.0 (RuStore, RS variant).
- **R8-карта**: пере-резолв 47 классов drift 26.23.2 → 26.24.0 + полная перекрёстная верификация всех патчей.

---

## ✅ Smoke-тест (2026-07-24)

- Устройство: Samsung S25 (`RFCX5013V5F`), Android 16.
- Основной (`ru.oneme.app`) — 0 FATAL, 0 VerifyError, 0 ClassCast.
- Клон (`ru.oneme.ap2`) — устанавливается, стартует, push принимает.
- `install -r` поверх предыдущей сборки мода → user-data сохранена, чистый старт.
- Проверено на устройстве:
  - **filterSettingsList** — новый sealed-фильтр отрабатывает, скрытие пунктов настроек без регрессий ✅.
  - **filterPageList / filterChatList** — миграция типов, скрытие сервисных чатов и «Только чаты» работают ✅.
  - **startOnNew** — открывается папка «Новые» в основном и в клоне (новый chokepoint) ✅.
  - **hide_phone Gate2** — номер скрыт (цепочка Gate2 пере-отрезолвлена) ✅.
  - **manual_update_check** — short-tap по версии открывает диалог проверки ✅.
  - **Toolbar «О приложении»** — long-press по версии открывает диалог тумблеров ✅.
  - **iconColor** — цвет иконки меняется на лету через обновлённый `resources.arsc` ✅.
  - **🗑 + секунды** на удалённых сообщениях ✅.

---

## 🔒 SHA-256 (V1)

```
b59b3d02f4215d6b115c78e203b3c732d3d23979276693ad5ab76bc83629d87b  MAX_26.24.0_arm64_V1.apk
e879ef9c6765dfc9a3f90a11155dc289b68813bb2db0dc7a74f0c5cb78c43f61  MAX_26.24.0_arm64_V1_clone.apk
b646898cab055ccad1ebf9cc7831af0798b2affa6335854b7f954104f66c9d89  MAX_26.24.0_arm7_V1.apk
99f625378e8a230384b8fc983d8db9f4cc5ca9fd3b4ba80ea5c83706e6d45e46  MAX_26.24.0_arm7_V1_clone.apk
```

---

## 📜 Лицензия

Только для личного использования, в учебных и исследовательских целях. Мод предоставляется «as is», подписан debug-ключом, не публикуется в Google Play. Правообладатель оригинального MAX — **ООО «МАХ» (VK)**; мод не аффилирован, не претендует на права на код / знаки / иконки. Использование может нарушать Условия сервиса — **на свой риск**.
