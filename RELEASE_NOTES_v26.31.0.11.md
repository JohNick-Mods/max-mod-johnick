# MAX Mod 26.31.0 RS — V11 (20.09.2026)

Хотфикс поверх V10.

## Исправления

- **Краш при входе в меню разработчика** — исправлен type-mismatch при чтении внутреннего флага `user-debug-report`.
- **Третий self-update путь** (`ForceUpdateScreen`) — отключён, показывал «Обновить приложение?» при каждом запуске в обход уже закрытых RuStore/HMS путей.
- **E2E-шифрование вложений** (голосовые/видео-кружки) — восстановлено после R8-дрейфа классов в стоке; шифрование при отправке и расшифровка при воспроизведении снова применяются.

Перед публикацией прогнан полный аудит всех патчей пайплайна.

Обновление с V10/V9/V8 — `install -r`, данные сохраняются.
