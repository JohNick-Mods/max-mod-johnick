# MAX Mod v26.24.0 V17 (RuStore)

**Сток:** 26.24.0 (RuStore, RS variant) · **Совместимость:** Android 8.0–16.0 (API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

Хотфикс поверх V10. **Весь функционал V10 сохранён, приватность усилена (расширена блокировка серверных PMS-флагов).** Ставится через `install -r` поверх любой сборки 26.x — данные сохраняются.

## 🆕 Что нового в V17 (относительно V10)

### 📄 Исправление открытия файлов/PDF в клоне (#FILE-1)
В клоне (`ru.oneme.ap2`) попытка открыть полученный файл или PDF приводила к ошибке доступа — `FileProvider` использовал захардкоженный authority основного пакета. Теперь authority строится динамически (`getPackageName() + ".provider"`) под каждый пакет — баг устранён.

### 🛡 Расширена нейтрализация серверных PMS-флагов
К списку принудительно нейтрализуемых серверных ключей (было 2: `net-ssl-session-validate`, `net-client-dns-enabled`) добавлено ещё 6: `mytracker-enabled`, `user-debug-report`, `tracer-non-fatal-crashed-enabled`, `show-vpn-call-bottomsheet`, `show-vpn-chat-bottomsheet`, `show-vpn-snackbar`.

### 🎨 Доработано покрытие адаптивных иконок
Добавлен недостающий цвет фона для синей иконки (была визуальная асимметрия с остальными 5 цветами), мипмап-XML покрывают больше плотностных конфигураций экрана.

---

## 📦 Из V8–V10 — сохранено

- **📞 Гудок дозвона V29**: `stop()` в момент реального ответа собеседника, атомарный `ToneGenerator`.
- **📦 Оптимизация размера APK** (V9): −17.6%.
- **🔔 Фикс повторных уведомлений при Антиудалении** (#PUSH-2): dedup-гейт `antiRead OR antiDelete` + throttle 30 сек.
- **🎨 Цвет уведомления = цвет иконки** (#ICON-1): тумблер `notifMatchIcon` (ON по умолчанию).
- **📌 Больше закреплённых чатов** (#PIN-1): тумблер `unlockPinLimit` (OFF по умолчанию, лимит до 10).
- PIN-блокировка, скорость воспроизведения, watch-фичи, невидимка, антиудаление, AMOLED, 6 цветов иконки, проверка обновлений, дев-меню, SMS-регистрация.

## 📱 SMS-регистрация — работает

WS-redirect от сервера MAX не блокируется. SMS-код доходит штатно.

## ⚠ Известные ограничения

- **Ringback**: ToneGenerator маршрутизируется через STREAM_VOICE_CALL; на устройствах без активной VoIP-сессии в момент DIALING гудок может быть тихим.
- **#PIN-1**: сервер может сбросить пины >2 при синхронизации.
- **AMOLED** — покрытие ~95%.
- **Watch-фичи** активны только при `widthPixels < 500`.

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.24.0_arm64_V17.apk` | Основной аккаунт, arm64-v8a (большинство современных телефонов). |
| `MAX_26.24.0_arm7_V17.apk` | Основной аккаунт, armeabi-v7a (старые устройства). |
| `MAX_26.24.0_arm64_V17_clone.apk` | Клон arm64. Оранжевая иконка, пакет `ru.oneme.ap2`. |
| `MAX_26.24.0_arm7_V17_clone.apk` | Клон arm7 (для WearOS / OnePlus Watch 3). |

**Клон (первая установка):** после установки — один раз Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS.

## Подпись

- apksigner **v2 + v3** (без v1). Cert SHA-1: `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.
- Smoke: Samsung S25 (`RFCX5013V5F`), Android 15, arm64 — main + clone без FATAL / VerifyError.

## SHA-256

```
2b98dc6c345265a26c30b63481db7743ef014516021ceeebcf326a1000536571  MAX_26.24.0_arm64_V17.apk
872d36372bd326a78bd937f4505ca1302f8b406d8c0bfbe66e01e6d1e1e95b1e  MAX_26.24.0_arm64_V17_clone.apk
c52fbbf58fa3d2ec895caed5617df972e87f5d89c50b982daf1effee017bc3fa  MAX_26.24.0_arm7_V17.apk
891c83021f57bfc19e992a666e52bcfcbe3dbad3d5a3618565f41f6c1a65d14d  MAX_26.24.0_arm7_V17_clone.apk
```

---

Правообладатель оригинального MAX — **ООО «МАХ» (VK)**. Мод не аффилирован, не является официальным продуктом. Использование может нарушать Условия сервиса — **на свой риск**.
