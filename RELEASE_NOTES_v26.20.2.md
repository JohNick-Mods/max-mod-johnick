# MAX 26.20.2 — Mods by JohNick V2

Универсальная сборка (GP) · arm64 + arm7 · Main + Clone · 4 APK · build 6758

## Что нового (vs 26.18.1)

- **🆕 Перенос на свежий сток 26.20.2 (build 6758).** R8-имена сверены по содержимому, все патчи портированы и проверены регрессией + smoke S25.
- **✅ Восстановлен «Цифровой ID» (Госуслуги / ЕСИА).** Привязка создаётся. `handleDeeplink` сохранён через безопасный passthrough — телеметрия мертва, а ссылки работают.
- **🆕 15-й тумблер: `blockMobileIdVerify`** — скрыть номер от оператора (Header Enrichment / Mobile ID). На «Цифровой ID» не влияет (разные механизмы).
- **✅ `startOnNew`** доведён до полноценной работы в основном приложении и в клоне.
- **🛡️ NSC:** операторские HE-домены закрыты (`trace-flow.ru`, `vk-analytics.ru`, `tracker.my.com`, `top-fwz1.mail.ru`, `apptracer.ru`), гос-эндпоинты (`voskhod.ru`, `digital.gov.ru`) оставлены для Цифрового ID.
- **✅ FCM-фильтр** — `MessageRemoved` / `ChatMessageRemoved` гейтятся через `antiDelete`. `LocationRequest` и `TamtamSpam` блокируются всегда.

## Какой файл качать

| Файл | Когда брать |
|---|---|
| **`MAX_26.20.2_arm64_V2.apk`** | **95% устройств** — современные с 2018 г. Samsung / Xiaomi / Realme / Huawei / Pixel |
| `MAX_26.20.2_arm7_V2.apk` | Старые 32-битные (до 2017 г.) |
| `MAX_26.20.2_arm64_V2_clone.apk` | Клон для второго аккаунта — оранжевая иконка, пакет `ru.oneme.ap2` |
| `MAX_26.20.2_arm7_V2_clone.apk` | То же, для 32-битных |
| `SHA256SUMS` | Контрольные суммы |
| `AUDIT_26.20.2_V2.pdf` | Полный технический аудит — 13/13 угроз нейтрализованы |
| `DIGITAL_ID_README.md` | Как включить «Цифровой ID», если открывается браузер |

## Установка

**Обновление с любой 26.x от Mods by JohNick:** ставится поверх, данные сохраняются. Cert SHA-1 `d6a7d757…1298b` — одинаков. **Не удаляйте** предыдущую версию.

**Первая установка:** разрешить установку из неизвестных источников → открыть APK → «Установить» → войти по номеру.

**Если стоит оригинальный MAX:** удалить перед установкой основного мода (пакет совпадает). Клон можно ставить рядом с оригиналом.

## Цифровой ID — если открывается браузер

Внутренний фикс встроен — обычно создаётся сразу. Из-за переподписи Android может не доверять автоматическому открытию ссылок `max.ru`. Лечится один раз:

- В приложении: попап «Цифровой ID» → «Открыть настройки» → «Открывать поддерживаемые ссылки»
- Или: **Настройки телефона → Приложения → MAX → «Открывать по умолчанию» → включить**
- Или через ADB:
  ```
  adb shell pm set-app-links-user-selection --user 0 --package ru.oneme.app true max.ru
  ```

Подробности — `DIGITAL_ID_README.md`.

## Подпись и стабильность

- v2 + v3 (без v1), проверена `apksigner`
- 168 регрессионных pytest-тестов + проверка целостности critical layers + smoke-тест Samsung S25 (Android 15) → **PASS**

## Нейтрализовано — 13 / 13 угроз аудита

DPS (`trace-flow.ru`) · myTracker (`vk-analytics.ru`) · OneLog (`api.ok.ru`) · Контакты в фоне · Boot-загрузка · cleartext HE-домены · `libtracernative.so` · скрытый MSISDN · push-телеметрия · и другие. Полный список — `AUDIT_26.20.2_V2.pdf`.
