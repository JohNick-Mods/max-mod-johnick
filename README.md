# MAX Mod by JohNick

[![Latest Release](https://img.shields.io/github/v/release/JohNick-Mods/max-mod-johnick?label=Последняя%20версия&color=blue)](https://github.com/JohNick-Mods/max-mod-johnick/releases/latest)
[![Downloads](https://img.shields.io/github/downloads/JohNick-Mods/max-mod-johnick/total?label=Скачиваний&color=green)](https://github.com/JohNick-Mods/max-mod-johnick/releases)
[![Issues](https://img.shields.io/github/issues/JohNick-Mods/max-mod-johnick?label=Issues)](https://github.com/JohNick-Mods/max-mod-johnick/issues)
[![Discussions](https://img.shields.io/github/discussions/JohNick-Mods/max-mod-johnick?label=Обсуждения)](https://github.com/JohNick-Mods/max-mod-johnick/discussions)

Модифицированная сборка мессенджера **MAX** (`ru.oneme.app`) с упором на приватность, нейтрализацию модулей телеметрии и пользовательские UX-функции.

> Проект создан исключительно в **учебных и исследовательских целях** в области информационной безопасности. Все правки выполнены статическим перепатчем APK и подписаны собственным ключом разработчика. Демонстрирует техническую возможность анализа и нейтрализации модулей телеметрии — **не предназначен для нанесения ущерба правообладателю**.

---

## Актуальная версия — **26.24.0 RS V17**

Хотфикс поверх V10: исправлено открытие файлов/PDF в клоне, расширена блокировка серверной телеметрии. **Весь функционал V10 сохранён, приватность усилена.** Ставится через `install -r` поверх любой сборки 26.x — данные сохраняются.

### 🆕 Что нового в V17

- **📄 Исправление открытия файлов/PDF в клоне.** `FileProvider` использовал захардкоженный authority основного пакета — в клоне (`ru.oneme.ap2`) это приводило к ошибке доступа при открытии полученных файлов. Теперь authority строится динамически под каждый пакет.
- **🛡 Расширена нейтрализация серверных PMS-флагов.** К списку принудительно заблокированных серверных ключей (было 2) добавлено ещё 6: `mytracker-enabled`, `user-debug-report`, `tracer-non-fatal-crashed-enabled`, `show-vpn-call-bottomsheet`, `show-vpn-chat-bottomsheet`, `show-vpn-snackbar`.
- **🎨 Доработано покрытие адаптивных иконок.** Синяя иконка теперь генерируется единообразно с остальными 5 цветами, иконки покрывают больше плотностных конфигураций экрана.

### 📞 Из V9–V10 — сохранено

- **📦 Оптимизация размера APK (V9).** Сжатие при репаке поднято до max DEFLATE (level 9). Размер APK уменьшен на **−17.6%** (arm64: ~40 МБ → ~33 МБ). Совместимость не изменилась.
- **📞 Исправление непрерывного гудка дозвона (V10).** В V8 и ранее гудок обрывался раньше реального ответа: `stop()` вызывался в `fc1.b()` (markActive), тогда как Android Telecom закрывал audio session с задержкой ~2 сек. Исправлено: `stop()` перенесён в `d25.onCallAccepted()` — точку реального ответа. Реализация упрощена до атомарного `ToneGenerator(STREAM_VOICE_CALL, 80).startTone(23, -1)`.

### 📞 Из V8 — сохранено

- Фикс повторных уведомлений при Антиудалении (dedup `antiRead OR antiDelete` + throttle 30 сек). Цвет уведомления = цвет иконки (тумблер `notifMatchIcon`, ON по умолчанию). Больше закреплённых чатов до 10 (тумблер `unlockPinLimit`, OFF по умолчанию). Гудок на исходящих. PIN-блокировка запуска + биометрия. Скорость воспроизведения голосовых/кружков.

### ⌚ Из V4 — сохранено (watch-фичи)

- **📐 Bounded ScrollView в диалогах мода на часах.** Высота ScrollView ограничена 55% `heightPixels` — кнопки не уезжают за экран на watchface 466×466.
- **📋 Точечное скрытие `topIndicatorView` (id `0x7f090908`) на часах.** `postDelayed(600ms)` + гейт `widthPixels<500`. Только `setVisibility`, скролл не затрагивается.
- **🎯 UpdateChecker выбирает ассет по ABI.** `Build.SUPPORTED_ABIS[0]` → `"arm7"` / `"arm64"`. Двойной фильтр (ABI + isClone).
- **🔘 Три кнопки в диалоге проверки обновления на часах.** «Нет / Да / Скачать последнюю» в один ряд.
- **🤝 WearOS wearable-метаданные в манифесте.** `standalone=true`, `notificationBridgeMode=NO_BRIDGING`.

### 🐞 Из V3 — сохранено

- **📎 Отправка фото/файлов** (пере-отрезолвленная R8-ссылка в патче вложений).
- **🖍 Читаемость окон обновления** (белый текст на фиксированно-тёмном фоне `#2D2D2D`, включая рамки чекбоксов).

### 🆕 Из V2 — UI-рефактор диалогов (сохранён)

- **🔵 Закруглённые диалоги** (16dp) во всех диалогах мода.
- **🎨 Выбор цвета иконки — нативные RadioButton** (6 цветов, применяется на лету, переживает перезапуск).
- **🧩 Класс `DialogsUtils`** — единый тема-детект + закруглённый показ + тонирование текста.
- **📐 Экран настроек мода переписан (−63% кода)** — хелперы `addSwitchRow` / `showDialog`.
- **🌗 Тема диалогов — по системному night-mode** (R8-независимо).

> Все правки V3 — только в UI-слое и в одном патче вложений; сеть, телеметрия, приватность и подпись без изменений относительно V1/V2.

### 🔧 База V1 (порт под сток 26.24.0)

Порт под сток 26.24.0 (RuStore), R8-rebump 47 классов, `filterSettingsList` под новую sealed-архитектуру, миграция фильтров чатов/страниц (`filterChatList`/`filterPageList`), `startOnNew` на новом chokepoint `c1/wi3.smali`, `hide_phone` Gate2, `manual_update_check` + long-press дев-меню, icon-switcher `resources.arsc` (stable-ids).

### ✅ Сохранено из 26.23.2 V1

- **🎨 «Цвет иконки» — 6 цветов** (синий/красный/зелёный/фиолетовый/чёрный/оранжевый), меняется на лету без переустановки.
- **📤 Порядок отправленных при «Невидимке»** — пересортировка на входе адаптера (gated `invisible=ON`).
- **🕹 «Невидимка» — единый мастер-тумблер** каскадно управляет `antiRead` / `antiTyping` / `antiPresence`.
- **🛡 Крашфиксы** при запуске (`startOnNew`) и при открытии вкладки «Чаты» (`Resources$NotFoundException`).
- **⌚ Watch-mode** — авто-адаптация плотности под маленькие экраны (умные часы), SMS-код на часах умещается.
- **🌑 AMOLED-тема** — чёрный `#000000` фон, авто при системной тёмной теме.
- **📲 Push у клона стабилен** (собственный `device_id`).
- **🔕 Нет push об удалённых** при `antiDelete=ON`. **⏱→✓** часы на исходящих.
- **🔎 Проверка обновлений** (short-tap) + **автопроверка opt-in** (по умолчанию OFF).

### 🗑 Всегда (26.23.2 V1)

- ⏱ **Время удаления на удалённых** — текстовый маркер «· удалено ЧЧ:ММ ДД.ММ» у строки времени, всегда, без тумблера.
- 🔢 **Счётчик непрочитанных** не накручивается на холодном старте.

### ⚠ Известные регрессии в 26.24.0 RS (сохранены из 26.23.2, не перенесены)

- **Префикс 🗑️ у текста удалённого сообщения отсутствует.** В 26.23.2 RS был отдельный патч (`apply_v_antidel_trash_prefix.py`), добавлявший эмодзи-корзину прямо в текст удалённого сообщения. При переносе на 26.24.0 RS скрипт не был портирован — сейчас показывается только текстовый маркер времени удаления (см. выше), без иконки. Планируется перенос в следующей версии.
- **Пункт «Дизайн система» в Dev-меню не скрыт.** Ведёт на пустой чёрный экран (`ShowroomScreen`, `stub_release` — вендор вырезал UI-каталог из релизной сборки; это дефект **стока**, не мода). Патч на скрытие пункта существует (`apply_v_devmenu_hide_showroom.py`), но по решению разработчика намеренно отключён — задокументирован как известный дефект, а не пофикшен.

### ⚠ Известные ограничения 26.24.0 RS V4

- **AMOLED** — покрытие ~95%: список чатов и всплывающие шторки могут остаться тёмно-серыми.
- **Диалоги мода — тёмная схема**: фон диалогов мода фиксированно тёмный (для гарантии контраста), даже при светлой системной теме.
- **Watch-фичи V4** активны только на устройствах с `widthPixels < 500` (runtime-гейт). На телефонах инертны.
- **Иконка 🗑️ у текста удалённого сообщения не отображается** (регрессия из 26.23.2 RS, патч не перенесён — виден только текстовый маркер времени удаления).
- **Пункт «Дизайн система» в Dev-меню не скрыт** — открывает пустой чёрный экран (дефект стока, скрытие отключено разработчиком намеренно).

### 🤔 Если видишь красный «!» на исходящих или чат открывается не внизу

Проверь тумблер **«Невидимка»** (`invisible`) — это мастер-переключатель, который каскадно взводит `smartAntiRead`, `antiTyping`, `antiPresence`. При `invisible=ON`:
- отметка «прочитано» уходит собеседнику **только после вашего ответа** — собеседник видит непрочитанным, поэтому чат может открываться не в самом низу;
- возможны отдельные ложные «!» на исходящих (когда сообщение реально доставлено, но сервер прислал спорный ответ).

Нужно стандартное поведение — выключи `invisible` и убедись что `smartAntiRead` тоже OFF.

📦 [**Скачать последнюю сборку →**](../../releases/latest)

---

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.24.0_arm64_V17.apk` | Основной аккаунт, arm64-v8a (большинство современных телефонов). **Если не уверены — этот.** |
| `MAX_26.24.0_arm7_V17.apk` | Основной аккаунт, armeabi-v7a (старые устройства). |
| `MAX_26.24.0_arm64_V17_clone.apk` | Клон arm64. Оранжевая иконка, пакет `ru.oneme.ap2`. |
| `MAX_26.24.0_arm7_V17_clone.apk` | Клон arm7. |

`SHA256SUMS` — контрольные суммы для проверки.

**Минимальный Android:** 8.0 (API 26). На 5.0/6.0/7.x установка сорвётся с ошибкой «Сбой при разборе пакета» — это ограничение стока, не мода.

---

## Установка

**Обновление с прошлой версии мода:** ставится поверх `install -r` (подпись та же). **Не удаляйте** предыдущую версию.

**Первая установка:**
1. Разрешить установку из неизвестных источников.
2. Скачать нужный APK из [Releases](../../releases/latest).
3. Открыть APK → «Установить».
4. Войти по номеру телефона.

**Клон (первая установка):** после установки APK клона — **один раз** Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS. Далее не требуется.

---

## 29 переключателей + выбор цвета иконки

**Long-press** по строке версии (Настройки → О приложении) откроет диалог с тумблерами. **Short-tap** — «Проверить обновления». Все по умолчанию **OFF** (кроме `amoledTheme` — авто при тёмной теме) — до первого включения мод ведёт себя идентично стоку (антидетект). Диалог следует системной теме.

### 🔒 Приватность
- **antiDelete** — антиудалялка (4 слоя + офлайн-персист + маркер 🗑 и время `ЧЧ:ММ:СС`).
- **invisible** — «Невидимка»: мастер-переключатель, взводит дочерние флаги ниже.
  - **antiTyping** — собеседник не видит набор текста.
  - **antiRead** — собеседник не видит отметку «прочитано» + внутричатовый счётчик обнулён + скролл в конец при открытии.
  - **smartAntiRead** — «Умная античиталка»: «прочитано» уходит только **после вашего ответа** (взаимоисключима с обычной).
- **antiPresence** — онлайн / last-seen скрыты (сервер видит offline). **Независимый тумблер**.
- **blockMobileIdVerify** — скрыть номер от оператора (Header Enrichment / Mobile ID). Ломает «Цифровой ID».
- **hidePhoneNumber** — скрыть номер в профиле (только отображение).
- **hideDigitalId** — скрыть пункт «Цифровой ID» из настроек **+ hero-баннер «Госуслуги»**.
- **gateOpcode5** — блокирует принудительный logout от сервера (WS-опкод 5).

### 📁 Папки и вкладки
- **hideAll** — скрыть папку «Всё».
- **hideSferum** — вкладка «Сферум» + пункт «Войти в Сферум» в настройках.
- **hideFavorites** — скрыть папку и чат «Избранное».
- **hideBusinessPromo** — скрыть «MAX для бизнеса» в Settings.
- **hideInviteFriends** — скрыть «Пригласить друзей» в меню.
- **reorderTabs** — «Чаты» первыми вместо «Контактов».
- **startOnNew** — старт на папке «Новые» (в основном и в клоне).

### 💬 Сервисные чаты
- **hideVerifyCodes**, **hideMaxOfficial**, **hideInteresting**, **hideUsefulNotifs**, **hideMaxBusiness** — скрыть соответствующие сервисные чаты.
- **hidePromoWidgets** — скрыть серверные промо-баннеры в списке чатов.
- **hideInviteLinks** — скрыть ссылки «Пригласить по ссылке» / «Найти по номеру».
- **hideChannels** — «Только чаты»: спрятать каналы (главный список + кастомные папки).
- **hideChannelRecs** — скрыть рекомендации каналов.

### 🔌 Системное и звонки
- **autoStart** — управление автозапуском в фоне (default OFF).
- **backCameraForNote** — видео-кружки с основной камеры вместо фронтальной.
- **debugMenu** — разблокирует скрытое инженерное меню звонков.
- **amoledTheme** — чисто чёрный фон `#000000` (окно, status-bar, nav-bar) в тёмной теме. Авто при системной тёмной теме.

### 🎨 Внешний вид
- **iconColor** — цвет иконки приложения: синий / красный / зелёный / фиолетовый / чёрный / оранжевый (radio, меняется на лету).

> Автопроверка обновлений управляется отдельным чекбоксом в окне проверки (не в общем списке). Watch-mode активируется автоматически по ширине экрана — тумблера нет. Тумблер `improveNotifications` присутствует в бинарнике (для двоичной совместимости), но **скрыт из UI** — crash-loop на OxygenOS 15.

---

## Watch-mode — как работает

Ничего включать не нужно. При каждом старте мод смотрит на ширину экрана в пикселях. Если она **меньше 500 px** (умные часы, компактные носимые устройства) — MAX автоматически переопределяет плотность экрана, из-за чего интерфейс, рассчитанный на телефон, ужимается и умещается на маленьком/круглом экране, не вылезая за края.

На телефонах и планшетах (ширина 1080 px+) режим **не активируется**. Локализация (язык интерфейса) при переключении плотности **сохраняется**.

---

## «Проверить обновления» — как работает

Настройки → О приложении → **короткий tap** по строке «Версия 26.24.0» → диалог с текущей и последней доступной версией. Запрос идёт в отдельный технический mirror-репозиторий (уменьшенная копия, только для проверки версии). Используется unauthenticated GitHub API — никаких ключей, никакой авторизации. GitHub видит только IP запроса и User-Agent.

**Автопроверка (opt-in):** в этом же окне — чекбокс «Автоматически проверять при запуске (не чаще раза в сутки)». **По умолчанию выключен** — пока не включён, при старте приложения никаких фоновых запросов проверки нет. Включён — раз в 24 часа при запуске идёт тихая проверка; диалог появится только если новая версия реально вышла. Без фоновых будильников/служб.

**Long-tap по версии** по-прежнему открывает дев-меню MAX.

---

## Антиудалялка — 4 слоя + офлайн-персист

1. **L3 — Central WS chat-event** — live-канал, когда собеседник удаляет прямо сейчас.
2. **L4 — Room ORM body-clear** — локальный SQL `UPDATE text=NULL` блокируется.
3. **V3 persistence** — удалённые сообщения остаются видимыми после ре-синка.
4. **Visible marker** — иконка 🗑 и время `ЧЧ:ММ:СС ДД.ММ` в строке времени (безусловно на рендере).
5. **Офлайн-персист (FCM-push)** — удаления, пришедшие пока приложение убито (OEM «Очистить всё» / Force stop / hard-kill), восстанавливаются на следующем старте.

---

## Цифровой ID (Госуслуги) — если не открывается

Внутренний фикс встроен в APK. Если браузер открывается с «Не нашли чат» — выполните **один раз**:

```bash
# основной
adb shell pm set-app-links-user-selection --user 0 --package ru.oneme.app true max.ru
# клон
adb shell pm set-app-links-user-selection --user 0 --package ru.oneme.ap2 true max.ru
```

Или вручную: Настройки → Приложения → MAX → «Открывать по умолчанию» → «Открывать поддерживаемые ссылки» → включить.

---

## Что нейтрализовано

- **DPS (Data Privacy Stats)** — `trace-flow.ru`. Stub `DpsInitProvider` + физическое удаление `libtracernative.so`.
- **myTracker (VK/Mail.ru)** — `vk-analytics.ru`. `initTracker`, `trackEvent`, `flush` заглушены.
- **Tracer-инициализаторы (7×)** и **Firebase Installations** — застаблены (SDK не стартует, нет HTTP к googleapis).
- **Контакты в фоне** — выгрузка адресной книги заглушена.
- **NSC** — операторские Header-Enrichment домены закрыты (`cleartext=false`).
- **WS-опкод 5 (LogoutCmd)** — под тумблер `gateOpcode5`.

---

## Подпись и совместимость

- Подпись **v2 + v3** (БЕЗ v1).
- Cert SHA-1: `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии (включая 26.23.0 GP) без потери данных.
- Smoke-тест: Samsung S25, Android 16 — main + clone без FATAL / VerifyError.

---

## История версий

| Версия | Дата | Ключевое |
|---|---|---|
| **26.24.0 RS V17** | 2026-07-29 | Исправлено открытие файлов/PDF в клоне (динамический FileProvider authority). Расширена нейтрализация серверных PMS-флагов (2 → 8 ключей). Доработано покрытие адаптивных иконок. |

---

## Дисклеймер

Правообладатель оригинального приложения MAX — **ООО «МАХ» (холдинг VK)**. Этот мод не аффилирован с ним, не является официальным продуктом. Использование модифицированных сборок может нарушать Условия использования сервиса — **используете на свой риск**.

Список изменений и деталей сборки — в [`RELEASE_NOTES_v26.24.0-V17.md`](RELEASE_NOTES_v26.24.0-V17.md) и в описании самого GitHub Release.
