# MAX Mod by JohNick

[![Latest Release](https://img.shields.io/github/v/release/JohNick-Mods/max-mod-johnick?label=Последняя%20версия&color=blue)](https://github.com/JohNick-Mods/max-mod-johnick/releases/latest)
[![Downloads](https://img.shields.io/github/downloads/JohNick-Mods/max-mod-johnick/total?label=Скачиваний&color=green)](https://github.com/JohNick-Mods/max-mod-johnick/releases)
[![Issues](https://img.shields.io/github/issues/JohNick-Mods/max-mod-johnick?label=Issues)](https://github.com/JohNick-Mods/max-mod-johnick/issues)
[![Discussions](https://img.shields.io/github/discussions/JohNick-Mods/max-mod-johnick?label=Обсуждения)](https://github.com/JohNick-Mods/max-mod-johnick/discussions)

Модифицированная сборка мессенджера **MAX** (`ru.oneme.app`) с упором на приватность, нейтрализацию модулей телеметрии и пользовательские UX-функции.

> Проект создан исключительно в **учебных и исследовательских целях** в области информационной безопасности. Все правки выполнены статическим перепатчем APK и подписаны собственным ключом разработчика. Демонстрирует техническую возможность анализа и нейтрализации модулей телеметрии — **не предназначен для нанесения ущерба правообладателю**.

---

## Актуальная версия — **26.23.0 GP V3** (build 6777)

Точечный релиз поверх V2: включён **рабочий Watch-mode** — авто-адаптация под смарт-часы. Всё из V2 (исправленная проверка обновлений, opt-in автопроверка) сохранено без изменений. Ставится поверх V2 через `install -r` без потери данных.

> **26.23.0 V3** (2026-07-17): реализован Watch-mode — если ширина экрана < 500 px (умные часы, WearOS-устройства), интерфейс автоматически переключается на плотность 240 dpi и умещается на маленьком экране. Без тумблера, на телефонах не активируется. Все фиксы V2 (исправление ложного «доступно обновление», opt-in автопроверка) сохранены.

### 🆕 Новое в V3

- **⌚ Watch-mode — авто-адаптация под смарт-часы.** При старте приложение читает ширину экрана в пикселях. Если она **< 500 px** (Huawei Watch, WearOS и т.п.) — плотность переключается на 240 dpi (hdpi), интерфейс ужимается до ~310 dp и умещается на круглом/маленьком экране, не вылезая за края. На телефонах (1080 px+) не активируется. Локализация при переключении сохраняется. Без тумблера — полностью автоматически.

### ✅ Сохранено из V2

- **🔎 Исправлена проверка обновлений.** Раньше короткий тап по версии показывал «доступно обновление», даже когда стоит самая свежая сборка. Теперь версия всегда синхронна со сборкой: при актуальной версии отвечает «Обновлений не обнаружено».
- **🔔 Автопроверка обновлений — чекбокс (по умолчанию ВЫКЛючен).** В окне проверки обновлений — пункт «Автоматически проверять при запуске (не чаще раза в сутки)». Пока не включён — приложение при старте **не делает ни одного сетевого запроса** проверки. При включении: раз в сутки — **тихая** проверка, диалог покажется **только если новая версия действительно есть**. Без фоновых служб.

### ✅ Сохранено из V1

- **🌑 AMOLED-тема** (`amoledTheme`) — чёрный `#000000` фон в тёмной теме, авто только при системной тёмной. Покрытие ~95%.
- **🔓 Антиприсутствие** (`antiPresence`) — независимый тумблер.
- Фикс краша античиталки при открытии чата · `hideInviteFriends`/`hideBusinessPromo` (сменились res-id) · антиудалённые не висят непрочитанными (#ANTIDEL-1) · дев-меню (long-press) и «Проверить обновления» (short-tap) переякорены · SMS-регистрация (User-Agent 26.23.0).

### ⚠ Известные ограничения 26.23.0 V3

- **AMOLED** — покрытие ~95%: список чатов и всплывающие шторки могут остаться тёмно-серыми.
- **#FILE-1** — PDF/видео/m4a из основного аккаунта временами отправляются с проблемами. Диагностика продолжается.

📦 [**Скачать последнюю сборку →**](../../releases/latest)

---

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.23.0_arm64_V3.apk` | Современные телефоны (2018+): Samsung, Xiaomi, Pixel, OnePlus… **Если не уверены — этот.** |
| `MAX_26.23.0_arm7_V3.apk` | Старые 32-битные устройства (до 2017 г.) |
| `MAX_26.23.0_arm64_V3_clone.apk` | Клон для второго аккаунта. Оранжевая иконка, пакет `ru.oneme.ap2` |
| `MAX_26.23.0_arm7_V3_clone.apk` | То же, для 32-битных |

`SHA256SUMS.txt` — контрольные суммы для проверки.

**Минимальный Android:** 8.0 (API 26). На 5.0/6.0/7.x установка сорвётся с ошибкой «Сбой при разборе пакета» — это ограничение стока, не мода.

---

## Установка

**Обновление с прошлой версии мода:** ставится поверх `install -r` (подпись та же). **Не удаляйте** предыдущую версию.

**Первая установка:**
1. Разрешить установку из неизвестных источников.
2. Скачать нужный APK из [Releases](../../releases/latest).
3. Открыть APK → «Установить».
4. Войти по номеру телефона.

**Клон (первая установка):** после установки APK клона — **один раз** Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS. Далее не требуется.

---

## 29 переключателей в настройках

**Long-press** по строке версии (Настройки → О приложении) откроет диалог с тумблерами. **Short-tap** — «Проверить обновления». Все по умолчанию **OFF** — до первого включения мод ведёт себя идентично стоку (антидетект). Диалог следует системной теме.

### 🔒 Приватность
- **antiDelete** — антиудалялка (4 слоя + офлайн-персист + маркер «· удалено HH:MM»).
- **invisible** — «Невидимка»: мастер-переключатель, взводит дочерние флаги ниже.
  - **antiTyping** — собеседник не видит набор текста.
  - **antiRead** — собеседник не видит отметку «прочитано» + внутричатовый счётчик обнулён + скролл в конец при открытии.
  - **smartAntiRead** — «Умная античиталка»: «прочитано» уходит только **после вашего ответа** (взаимоисключима с обычной).
- **antiPresence** — онлайн / last-seen скрыты (сервер видит offline). **Независимый тумблер** — можно включить отдельно от «Невидимки».
- **blockMobileIdVerify** — скрыть номер от оператора (Header Enrichment / Mobile ID). Ломает «Цифровой ID».
- **hidePhoneNumber** — скрыть номер в профиле (только отображение).
- **hideDigitalId** — скрыть пункт «Цифровой ID» из настроек **+ hero-баннер «Госуслуги»**.

### 📁 Папки и вкладки
- **hideAll** — скрыть папку «Всё».
- **hideSferum** — вкладка «Сферум» + пункт «Войти в Сферум» в настройках.
- **hideFavorites** — скрыть папку и чат «Избранное».
- **hideBusinessPromo** — скрыть «MAX для бизнеса» в Settings.
- **hideInviteFriends** — скрыть «Пригласить друзей» в меню.
- **reorderTabs** — «Чаты» первыми вместо «Контактов».
- **startOnNew** — старт на папке «Новые».

### 💬 Сервисные чаты
- **hideVerifyCodes**, **hideMaxOfficial**, **hideInteresting**, **hideUsefulNotifs**, **hideMaxBusiness** — скрыть соответствующие сервисные чаты.
- **hidePromoWidgets** — скрыть серверные промо-баннеры в списке чатов (баннер «MAX для бизнеса» с кнопкой «Открыть»).
- **hideInviteLinks** — скрыть ссылки «Пригласить по ссылке» / «Найти по номеру».
- **hideChannels** — «Только чаты»: спрятать каналы (главный список + кастомные папки).
- **hideChannelRecs** — скрыть рекомендации каналов.

### 🔌 Системное и звонки
- **autoStart** — управление автозапуском в фоне (default OFF).
- **backCameraForNote** — видео-кружки с основной камеры вместо фронтальной.
- **debugMenu** — разблокирует скрытое инженерное меню звонков.
- **gateOpcode5** — блокирует принудительный logout от сервера (WS-опкод 5).
- **amoledTheme** — чисто чёрный фон `#000000` (окно, status-bar, nav-bar) в тёмной теме, экономит батарею на OLED. Включается автоматически только при системной тёмной теме.

> Автопроверка обновлений управляется отдельным чекбоксом в окне проверки (не в общем списке). Watch-mode активируется автоматически по ширине экрана — тумблера нет. Тумблер `improveNotifications` присутствует в бинарнике (для двоичной совместимости), но **скрыт из UI** — crash-loop на OxygenOS 15. Вернётся в следующей версии.

---

## Watch-mode — как работает

Ничего включать не нужно. При каждом старте мод смотрит на ширину экрана в пикселях. Если она **меньше 500 px** (умные часы, компактные носимые устройства) — MAX автоматически переопределяет плотность экрана на **240 dpi**, из-за чего интерфейс, рассчитанный на телефон (~360 dp), ужимается до ~310 dp и умещается на маленьком/круглом экране, не вылезая за края.

На телефонах и планшетах (ширина 1080 px+) режим **не активируется** — обычные устройства работают ровно как раньше. Локализация (язык интерфейса) при переключении плотности **сохраняется**.

---

## «Проверить обновления» — как работает

Настройки → О приложении → **короткий tap** по строке «Версия 26.23.0» → диалог с текущей и последней доступной версией. Запрос идёт в отдельный технический mirror-репозиторий (уменьшенная копия, только для проверки версии). Используется unauthenticated GitHub API — никаких ключей, никакой авторизации. GitHub видит только IP запроса и User-Agent.

**Автопроверка (opt-in):** в этом же окне — чекбокс «Автоматически проверять при запуске (не чаще раза в сутки)». **По умолчанию выключен** — пока не включён, при старте приложения никаких фоновых запросов проверки нет. Включён — раз в 24 часа при запуске идёт тихая проверка; диалог появится только если новая версия реально вышла (нет обновления или нет сети → молча). Без фоновых будильников/служб — только одноразовая проверка при старте.

**Long-tap по версии** по-прежнему открывает дев-меню MAX.

---

## Антиудалялка — 4 слоя + офлайн-персист

1. **L3 — Central WS chat-event** — live-канал, когда собеседник удаляет прямо сейчас.
2. **L4 — Room ORM body-clear** — локальный SQL `UPDATE text=NULL` блокируется.
3. **V3 persistence** — удалённые сообщения остаются видимыми после ре-синка.
4. **Visible marker** — «· удалено HH:MM» в строке времени.
5. **Офлайн-персист (FCM-push)** — удаления, пришедшие пока приложение убито (OEM «Очистить всё» / Force stop / hard-kill), восстанавливаются на следующем старте.

---

## Цифровой ID (Госуслуги) — если не открывается

Внутренний фикс встроен в APK. Если браузер открывается с «Не нашли чат» — выполните **один раз**:

```bash
# основной
adb shell pm set-app-links-user-selection --user 0 --package ru.oneme.app true max.ru
# клон
adb shell pm set-app-links-user-selection --user 0 --package ru.oneme.ap2 true max.ru
```

Или вручную: Настройки → Приложения → MAX → «Открывать по умолчанию» → «Открывать поддерживаемые ссылки» → включить.

---

## Что нейтрализовано

- **DPS (Data Privacy Stats)** — `trace-flow.ru`. Stub `DpsInitProvider` + физическое удаление `libtracernative.so`.
- **myTracker (VK/Mail.ru)** — `vk-analytics.ru`. `initTracker`, `trackEvent`, `flush` заглушены.
- **OneLog (call analytics)** — `api.ok.ru`. `UploadService → return-void`.
- **Контакты в фоне** — выгрузка адресной книги заглушена.
- **NSC** — операторские Header-Enrichment домены закрыты (`cleartext=false`).
- **WS-опкод 5 (LogoutCmd)** — под тумблер `gateOpcode5`.

---

## Подпись и совместимость

- Подпись **v2 + v3** (БЕЗ v1).
- Cert SHA-1: `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии (включая V1/V2) без потери данных.
- Smoke-тест: Samsung S25, Android 16 — main + clone без FATAL / VerifyError.

---

## История версий

| Версия | Дата | Ключевое |
|---|---|---|
| **26.23.0 GP V3** | 2026-07-17 | Реализован Watch-mode — авто-адаптация под смарт-часы (ширина < 500 px → density 240 dpi, без тумблера, локализация сохраняется). Все фиксы V2 (исправление ложного «доступно обновление», opt-in автопроверка) сохранены. |

---

## Дисклеймер

Правообладатель оригинального приложения MAX — **ООО «МАХ» (холдинг VK)**. Этот мод не аффилирован с ним, не является официальным продуктом. Использование модифицированных сборок может нарушать Условия использования сервиса — **используете на свой риск**.

Все правки задокументированы в техническом аудите — [`AUDIT_26.23.0_V3.pdf`](AUDIT_26.23.0_V3.pdf) (markdown: [`AUDIT_26.23.0_V3.md`](AUDIT_26.23.0_V3.md)).
