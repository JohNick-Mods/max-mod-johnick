# MAX Mod by JohNick

[![Latest Release](https://img.shields.io/github/v/release/JohNick-Mods/max-mod-johnick?label=Последняя%20версия&color=blue)](https://github.com/JohNick-Mods/max-mod-johnick/releases/latest)
[![Downloads](https://img.shields.io/github/downloads/JohNick-Mods/max-mod-johnick/total?label=Скачиваний&color=green)](https://github.com/JohNick-Mods/max-mod-johnick/releases)
[![Issues](https://img.shields.io/github/issues/JohNick-Mods/max-mod-johnick?label=Issues)](https://github.com/JohNick-Mods/max-mod-johnick/issues)
[![Discussions](https://img.shields.io/github/discussions/JohNick-Mods/max-mod-johnick?label=Обсуждения)](https://github.com/JohNick-Mods/max-mod-johnick/discussions)

Модифицированная сборка мессенджера **MAX** (`ru.oneme.app`) с упором на приватность, нейтрализацию модулей телеметрии и пользовательские UX-функции.

> Проект создан исключительно в **учебных и исследовательских целях** в области информационной безопасности. Все правки выполнены статическим перепатчем APK и подписаны собственным ключом разработчика. Демонстрирует техническую возможность анализа и нейтрализации модулей телеметрии — **не предназначен для нанесения ущерба правообладателю**.

📦 [**Скачать последнюю сборку →**](../../releases/latest)

---

## Актуальная версия — **26.29.1 V11**

**Что нового в V11 (2026-09-09):**
- 🤝 **Тихий E2E-handshake.** Раньше первые 1-2 сообщения после сброса доверия или в новом чате уходили открытым текстом, пока собеседник не напишет сам. Теперь мод в фоне тихо обменивается служебными ключами сразу после появления контакта — рабочая защищённая сессия готова ещё до первого сообщения. Полностью незаметно.
- 🔓 **Честная индикация состояния шифрования.** В режиме «Авто» без установленной сессии текст явно говорит «Авто (нет сессии)» серым — вместо зелёного замка, который раньше показывал только намерение, а не факт.
- 🔔 **Плашки о причине отсутствия шифрования.** Несовместимая версия собеседника, установка сессии, сбой шифрования — теперь видимое служебное сообщение в чате вместо тихого отказа.
- 🎚 **Глобальный тумблер «E2E Шифрование»** в разделе «Приват» (по умолчанию ВЫКЛ). При ВЫКЛ мод ведёт себя как оригинал — никаких следов шифрования в интерфейсе.
- ✨ **Автовключение E2E** без ручных действий при первом сообщении от собеседника с таким же модом.
- ✅ **Тумблер «Кнопка «Прочитать всё»».** Кнопка массовой отметки чата прочитанным (✓✓ в шапке) теперь опциональна — Настройки MAX → Чаты → Прочее.
- 🔧 **Критичный фикс:** устранён бесконечный цикл ошибки расшифровки при смене ключа собеседника.
- 🔧 **Фикс асимметричного разрыва защищённой сессии** между направлениями переписки.
- 🔧 Плашка «Доверие сброшено» больше не залипает навсегда.
- 🔧 Устранён редкий краш при холодном старте.
- 🔧 Устранена утечка служебных control-токенов в текст сообщения при автообмене ключами.
- 🔧 Устранено накопление служебных плашек статуса E2E в чате.

Весь функционал V9 сохранён, включая сквозное E2E-шифрование личных чатов (Signal Protocol). Полный список изменений — [RELEASE_NOTES_v26.29.1.md](./RELEASE_NOTES_v26.29.1.md).

**Базовый APK:** MAX 26.29.1 (апстрим обновился с 26.27.1). Обновление с V9 — `install -r`, данные сохраняются.

---

## Какой APK качать

| Файл | Когда брать |
|---|---|
| `MAX_26.29.1_arm64_V11.apk` | Основной аккаунт, arm64-v8a (большинство современных телефонов). **Если не уверены — этот.** |
| `MAX_26.29.1_arm7_V11.apk` | Основной аккаунт, armeabi-v7a (старые устройства, WearOS). |
| `MAX_26.29.1_arm64_V11_clone.apk` | Клон arm64. Оранжевая иконка, пакет `ru.oneme.ap2`. |
| `MAX_26.29.1_arm7_V11_clone.apk` | Клон arm7 (для WearOS / OnePlus Watch 3). |

`SHA256SUMS` — контрольные суммы для проверки.

| Файл | SHA-256 |
|---|---|
| `MAX_26.29.1_arm64_V11.apk` | `c9e7c1d3d6690e333b50e65857a72277e70cd4c2f1e50f10ac96145effadadd1` |
| `MAX_26.29.1_arm64_V11_clone.apk` | `050f1fbf76d726bb8883a7a63818fc6b4cf3ae141263e1b6efe2b626f2f120f9` |
| `MAX_26.29.1_arm7_V11.apk` | `296cf123230de1055b84d7f833e04e23c255835c25b7f4d50c4476c89a1bc24b` |
| `MAX_26.29.1_arm7_V11_clone.apk` | `0cfe1e4c84ed3bfb0a04b7e23ec522590815906efd289956550b1e3ee824fecb` |

**Минимальный Android:** 8.0 (API 26).

---

## Установка

**Обновление с прошлой версии мода:** ставится поверх (`install -r`), данные сохраняются. Удалять не нужно.

**Первая установка:**
1. Разрешить установку из неизвестных источников.
2. Скачать нужный APK из [Releases](../../releases/latest).
3. Открыть APK → «Установить».
4. Войти по номеру телефона.

**Клон (первая установка):** после установки APK клона — **один раз** Настройки → Приложения → MAX Clone → «Очистить данные» → войти по SMS.

---

## Как открыть настройки мода

Два способа:
- **Long-press** по строке версии (Настройки → О приложении).
- **Кнопка ⚙** в правом нижнем углу списка чатов (если включён тумблер `showModFab`, по умолчанию ON).

**Short-tap** по версии — «Проверить обновления».

Все тумблеры по умолчанию **OFF** (кроме `showModFab` и `amoledTheme`) — до первого включения мод ведёт себя идентично стоку.

---

## Справочник по всем настройкам

### 🔒 Приватность

| Тумблер | Что делает |
|---|---|
| `antiDelete` | **Антиудаление.** Восстанавливает удалённые собеседником сообщения. 4 слоя перехвата + офлайн-персист (переживает hard-kill на Xiaomi/Realme/Huawei). На удалённом сообщении показывается время удаления `ЧЧ:ММ:СС ДД.ММ`. Push-уведомления об удалении не приходят. |
| `invisible` | **Невидимка — мастер-тумблер.** Включает все четыре дочерних флага ниже разом. Дочерние работают и независимо. |
| `antiTyping` | Собеседник не видит статус «печатает…». |
| `antiRead` | Сообщения не помечаются прочитанными. Взаимоисключим с `smartAntiRead`. |
| `smartAntiRead` | **Умная античиталка.** Отметка «прочитано» уходит собеседнику только после вашего ответа в чат. Взаимоисключима с `antiRead`. |
| `antiPresence` | Статус онлайн и «был(а)» скрыты — сервер видит вас offline. Независимый тумблер (не дочерний). |
| `hideStoryView` | Просмотры историй не отправляются на сервер. Вы по-прежнему видите чужие истории. Автоматически включается при `invisible=ON`. |
| `blockMobileIdVerify` | Блокирует скрытый MSISDN-запрос оператора (Mobile ID / Header Enrichment). **Внимание:** ломает «Цифровой ID» (Госуслуги). |
| `hidePhoneNumber` | Скрывает строку с номером телефона в профиле (только отображение). |
| `hideDigitalId` | Скрывает пункт «Цифровой ID» из настроек и hero-баннер Госуслуг. |
| `gateOpcode5` | Блокирует WS-команду принудительного разлогинивания (opcode 5, LogoutCmd) от сервера. Экспериментально. |
| `chatPin` | **PIN на отдельные чаты.** Запрашивает PIN при каждом входе в защищённый чат. Как защитить чат: включить тумблер → открыть чат → **долгое нажатие на заголовок** (имя вверху) → «🔒 Чат защищён PIN» (повторное долгое нажатие снимает). При первом входе в защищённый чат мод попросит задать PIN. Независим от `appLock` / `settingsPin`. |
| `unlockPinLimit` | *(скрыт в V5, включён по умолчанию)* Лимит закреплённых чатов снят «из коробки» без тумблера. Серверный лимит остаётся — сервер может сбросить лишние пины при синхронизации. |
| `sendStats` | **Анонимная статистика (opt-in, по умолчанию ON).** Один раз при запуске отправляет ping: версию мода и страну устройства (системная локаль). Никаких идентификаторов, аккаунтов и содержимого переписки. Помогает автору видеть реальную аудиторию и понимать, на каких версиях стока сидят пользователи. Отключается этим тумблером — ping не отправляется совсем. |

---

### 📁 Папки и вкладки

| Тумблер | Что делает |
|---|---|
| `hideAll` | Скрыть вкладку «Всё». |
| `hideSferum` | Скрыть вкладку «Сферум». |
| `hideFavorites` | Скрыть вкладку «Избранное». |
| `hideBusinessPromo` | Скрыть «MAX для бизнеса» в нижней навигации. |
| `hideInviteFriends` | Скрыть ссылку «Пригласить друзей». |
| `reorderTabs` | Переставить порядок нижних вкладок: Чаты → Звонки → Контакты (вместо стокового). |
| `hideTabChats` | Скрыть вкладку «Чаты» из нижней навигации. |
| `hideTabCalls` | Скрыть вкладку «Звонки» из нижней навигации. |
| `hideTabContacts` | Скрыть вкладку «Контакты» из нижней навигации. |
| `startOnNew` | Приложение открывается на вкладке «Новые» (работает в main и clone). |
| `hideStories` | Скрыть блок «Истории» (сторис) в шапке списка чатов. |
| `hideArchive` | Скрыть pinned-заголовок «Архив» из главного списка чатов. Архивированные чаты остаются, кнопка-заголовок не отображается. |

---

### 💬 Сервисные чаты

| Тумблер | Что делает |
|---|---|
| `hideVerifyCodes` | Скрыть чат «Коды подтверждения». |
| `hideMaxOfficial` | Скрыть официальный чат MAX. |
| `hideInteresting` | Скрыть чат «Интересное для вас». |
| `hideUsefulNotifs` | Скрыть чат «Полезные уведомления». |
| `hideMaxBusiness` | Скрыть чат «Max для бизнеса». |
| `hidePromoWidgets` | Скрыть промо-баннеры в списке чатов (серверные виджеты типа «MAX для бизнеса — Открыть»). |
| `hideInviteLinks` | Скрыть ссылки «Пригласить по ссылке» и «Найти по номеру» в списке чатов. |
| `hideChannels` | **«Только чаты»** — убрать каналы из списка и папок. |
| `hideChannelRecs` | Скрыть блок «Рекомендации» (чужие каналы для подписки) на вкладке «Каналы». |
| `hideBotChats` | Скрыть чаты типа BOT из списка. |
| `hidePayments` | Скрыть чат «Платежи». |
| `hideVKPromo` | Скрыть промо-чат VK. |
| `hideSferumChat` | Скрыть чат Сферум (отдельно от вкладки). |
| `hideAssistant` | Скрыть чат «Ассистент». |

---

### 🔌 Звонки и система

| Тумблер | Что делает |
|---|---|
| `autoStart` | Автозапуск MAX при перезагрузке устройства. |
| `playbackSpeed` | Скорость воспроизведения голосовых сообщений и видео: 0.5× / 0.75× / 1× / 1.25× / 1.5× / 2×. Применяется при каждом play(). |
| `backCameraForNote` | Видео-кружки записываются основной камерой (по умолчанию — фронтальная). |
| `debugMenu` | Разблокировать скрытое инженерное меню в звонках (кнопка «Ещё»). |
| `appLock` | **Блокировка PIN-кодом.** Запрашивает PIN при каждом холодном старте приложения. PIN задаётся при первом включении. |
| `appLockUseBiometric` | Использовать биометрию / PIN устройства (KeyguardManager) вместо PIN мода. При отмене — обычный PIN-диалог. Работает совместно с `appLock`. |
| `changePinAction` | Включите — при следующем старте появится диалог смены PIN (старый → новый → повтор). После смены тумблер автоматически выключится. |
| `settingsPin` | **PIN на настройки мода.** Отдельный PIN для входа в диалог настроек, независим от `appLock`. |
| `changeSettingsPinAction` | Смена PIN настроек мода (аналогично `changePinAction`, но для Settings PIN). |
| `showModFab` | **FAB ⚙** — плавающая кнопка в правом нижнем углу списка чатов для быстрого доступа к настройкам. По умолчанию ON. |

---

### 👪 Родительский контроль

Все тумблеры этой категории работают при включённом мастер-тумблере `parental_master` **или** соответствующем тумблере в других категориях (OR-гейт).

| Тумблер | Что делает |
|---|---|
| `parental_master` | Мастер-тумблер родительского контроля — включает все ограничения ниже разом. |
| `parental_noOutgoingCalls` | Запретить исходящие звонки. |
| `parental_noNewChats` | Запретить создание новых чатов. |
| `parental_noForward` | Запретить пересылку сообщений. |
| `parental_hideChannels` | Скрыть каналы (аналог `hideChannels`, активируется при `parental_master=ON`). |
| `parental_hidePayments` | Скрыть платёжные чаты (аналог `hidePayments`). |
| `parental_hideVKPromo` | Скрыть VK-промо (аналог `hideVKPromo`). |
| `parental_timeLock` | **Блокировка по расписанию.** Запрещает вход в приложение в заданном диапазоне времени. Диапазон задаётся через два TimePickerDialog (начало и конец) — может пересекать полночь (например, 22:00–07:00). |

---

### 🎨 Внешний вид

| Тумблер / элемент | Что делает |
|---|---|
| `iconColor` | Цвет иконки приложения: Синий / Красный / Зелёный / Фиолетовый / Чёрный. Применяется на лету без переустановки. |
| `hideReactions` | Полностью скрыть emoji-реакции под сообщениями. |
| `settingsUiMode` | Режим отображения настроек мода: **0** — Табы (классика), **1** — Вертикальный список, **2** — Единый плоский список. |
| 🌞 / 🌙 / 🖤 (кнопки темы) | Переключение темы оформления: Светлая / Тёмная / AMOLED (#000000). |

---

## FAQ

### ❓ Вижу красный «!» на исходящих или чат открывается не внизу

Проверь тумблер **`invisible`** — он каскадно включает `smartAntiRead`, `antiTyping`, `antiPresence`. При `smartAntiRead=ON` отметка «прочитано» уходит только после вашего ответа — собеседник видит непрочитанным, поэтому чат может открываться не в самом низу. Если нужно стандартное поведение — выключи `invisible` и убедись что `smartAntiRead` тоже OFF.

### ❓ Не приходит SMS при первой регистрации

Убедись, что устанавливаешь версию для правильной архитектуры (arm64 для современных телефонов). Мод встроил подлинные константы отпечатка DEX/SO/SIG из оригинального APK — SMS должна приходить. Если нет — напиши в [Issues](../../issues).

### ❓ «Цифровой ID» (Госуслуги) не открывается, браузер показывает «Не нашли чат»

Выполни один раз:

```bash
# основной аккаунт
adb shell pm set-app-links-user-selection --user 0 --package ru.oneme.app true max.ru
# клон
adb shell pm set-app-links-user-selection --user 0 --package ru.oneme.ap2 true max.ru
```

Или вручную: Настройки → Приложения → MAX → «Открывать по умолчанию» → «Открывать поддерживаемые ссылки» → включить.

> Если включён тумблер `blockMobileIdVerify` — Цифровой ID работать не будет, это ожидаемо. Выключи тумблер перед входом через Госуслуги.

### ❓ Watch-mode — как работает

Ничего включать не нужно. При каждом старте мод смотрит на ширину экрана. Если она **меньше 500 px** (умные часы, носимые) — MAX автоматически переопределяет плотность, интерфейс ужимается и умещается на маленьком/круглом экране. На телефонах и планшетах (1080 px+) режим **не активируется**. Тумблера нет.

### ❓ Проверка и автопроверка обновлений

Настройки → О приложении → **короткий tap** по строке версии. В этом же окне — чекбокс **«Автоматически проверять при запуске (не чаще раза в сутки)»** (по умолчанию выключен). При включении раз в 24 часа мод тихо проверяет GitHub; диалог появится только если новая версия вышла. Без фоновых служб. **Long-tap** по версии — инженерное дев-меню MAX.

### ❓ Клон не получает push-уведомления

Клон использует собственный `device_id` и собственный токен. Если push пропали — попробуй: Настройки устройства → Приложения → MAX Clone → «Очистить кэш». Если не помогло — пересоздай аккаунт клона («Очистить данные» → войти заново по SMS).

---

## Что нейтрализовано (всегда, без тумблеров)

- **DPS (Data Privacy Stats)** — `trace-flow.ru`. Stub `DpsInitProvider` + физическое удаление `libtracernative.so`.
- **myTracker (VK/Mail.ru)** — `vk-analytics.ru`. `initTracker`, `trackEvent`, `flush` заглушены.
- **Tracer-инициализаторы (7×)** и **Firebase Installations** — застаблены (SDK не стартует).
- **Контакты в фоне** — выгрузка адресной книги заглушена.
- **NSC** — операторские Header-Enrichment домены закрыты (`cleartext=false`).
- **Счётчик непрочитанных** не накручивается на холодном старте.
- **Время удаления** (`ЧЧ:ММ:СС ДД.ММ`) на удалённых сообщениях — всегда, без тумблера.
- **Гудок дозвона** на исходящих — непрерывный до ответа собеседника, без тумблера.
- **Ручной порядок закреплённых чатов** — перетаскивание за иконку-булавку 📌 вверх/вниз, порядок хранится локально. Включено всегда, тумблера нет.
- **Кнопка «✓✓ Прочитать»** — плавающая перетаскиваемая кнопка в открытом чате (независимая от `showModFab`). Один тап отмечает чат прочитанным «в обход» `invisible`/antiRead/smartAntiRead (одноразово, без отключения самих тумблеров) и гасит залипшую иконку реакции на одно сообщение. Стартовая позиция — сразу слева от кнопки «Позвонить» в тулбаре чата, кнопку можно перетащить в любое удобное место — позиция запоминается. Цвет фона/обводки/текста подстраивается под тему (тёмная/светлая). Включена всегда, тумблера нет.
- **Акцентный цвет уведомления** — подстраивается под выбранный цвет иконки приложения. ⚠️ Меняет только акцент/фон в *развёрнутом* уведомлении (заголовок, круглая подложка); саму маленькую иконку в шторке (status bar) Android рисует монохромной у всех приложений начиная с версии 5.0 — это системное ограничение ОС, ни один мод не может его обойти.
- **WS-опкод 5 (LogoutCmd)** — под тумблер `gateOpcode5`.

---

## Подпись и совместимость

- Подпись **v2 + v3** (без v1).
- Cert SHA-1: `d6a7d757fa383af131263eb585d4e2be4da1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии без потери данных.
- Тестовый стенд: Samsung Galaxy S25, Android 15, arm64.
- Верификация сборки 26.29.1 V11: полный fan-out аудит всех 248 патч-скриптов пайплайна по 9 тематическим группам параллельными агентами (обязательное HARD-RULE перед публикацией) — найдены и исправлены 2 бага (R8-drift отключённого патча badge-ghost, silent-fail в flat-settings роутере). Динамический smoke: тихий E2E-handshake, честная индикация состояния сессии, служебные плашки причины отказа E2E, глобальный тумблер `e2eEncryption`, автовключение E2E — device-verified на S25 + Redmi (шифрование сообщений в обе стороны, без крашей). `apksigner verify` v2+v3, distinct ABI SHA. Экспериментальное шифрование звонков, заявленное в V9 alpha, в V11 не проверялось как рабочая функция и не входит в текущее публичное описание.

---

## История версий

| Версия | Дата | Ключевое |
|---|---|---|
| **26.29.1 V11** | 2026-09-09 | Тихий двухраундовый E2E-handshake (сессия готова до первого сообщения). Честная индикация состояния шифрования («Авто (нет сессии)»). Служебные плашки о причине отсутствия шифрования. Глобальный тумблер «E2E Шифрование» (раздел «Приват», default OFF). Автовключение E2E при первом сообщении собеседника с модом. Тумблер «Кнопка «Прочитать всё»». Фикс бесконечного цикла ошибки расшифровки при смене ключа. Фикс асимметричного разрыва сессии. Фикс залипающей плашки сброса доверия. Фикс краша при холодном старте. Фикс утечки control-токенов в текст сообщения. Фикс накопления служебных плашек E2E-статуса. |

---

## Дисклеймер

Правообладатель оригинального приложения MAX — **ООО «МАХ» (холдинг VK)**. Этот мод не аффилирован с ним, не является официальным продуктом. Использование модифицированных сборок может нарушать Условия использования сервиса — **используете на свой риск**.

Технический аудит патчей: [AUDIT_26.29.1_V11.pdf](./AUDIT_26.29.1_V11.pdf).

Технический аудит изменений — в файле `AUDIT_26.27.1_V9.pdf` в ассетах Release.
