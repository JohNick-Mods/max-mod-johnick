# MAX 26.27.1 V3 — Технический аудит мода

**Версия стока**: 26.27.1 (build 6802)  
**Версия мода**: V3 (Privacy Mod by JohNick)  
**Дата**: 2026-08-16  
**Совместимость**: `ru.oneme.app` (Android 8.0–16.0 / API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

> **V3**: догоняющий хотфикс поверх V2. Две правки по итогам тестирования: (1) тумблер Chat-PIN был скрыт и не отображался в настройках — открыт; (2) перетаскивание закреплённых чатов за pin-иконку не начиналось — обработчик касания не потреблял `ACTION_DOWN`, исправлено. Весь функционал V2 сохранён.

---

## 🆕 26.27.1 V3 — что исправлено

### 🔑 Chat-PIN — тумблер теперь виден в настройках
В V2 тумблер «🔒 PIN на отдельные чаты» имел флаг `hidden` (остался помеченным как «отложено» с ранних сборок, когда фича была не готова). Из-за этого он не рендерился в UI, и гейт входа-по-PIN (`ChatScreen.onViewCreated` → `sget-boolean AppFlags->chatPin:Z`) никогда не активировался через интерфейс. Флаг `hidden` снят — тумблер отображается в категории **Приватность**. Само поле `chatPin:Z` в `AppFlags` и логика гейта не менялись, изменение чисто UI-видимости.

### 📌 Перетаскивание закреплённых чатов за pin-иконку
Обработчик касания на pin-иконке закреплённой строки (`Lone/me/util/PinHandleTouch;->onTouch`) на `ACTION_DOWN` вызывал `AppFlags.pinStartDrag(vh)`, но возвращал `false` — не потреблял событие. Из-за этого родительский `ViewPager2`/`RecyclerView` перехватывал жест как горизонтальный свайп/скролл, и `ItemTouchHelper` не получал контроль над перетаскиванием. Исправлено: `onTouch` возвращает `true` (`const/4 v1, 0x1`) после `pinStartDrag` на `ACTION_DOWN`, потребляя жест — drag корректно стартует. Прочие action-коды по-прежнему возвращают `false`.

**Перетаскивание работает всегда, без тумблера** (флаг `pinnedSort` имеет `default: true` и не выводится в UI — включение безусловное). Чтобы переупорядочить — зажать иконку-булавку на закреплённом чате и тащить вверх/вниз; порядок хранится локально.

---

## 🆕 Унаследовано из 26.27.1 V2 (без изменений)

### 🔔 Гудок дозвона больше не «залипает»
Обработчики жизненного цикла вызова переименовались в стоке (`td1→qe1` для destroy/disconnect, `m55→h75` для onCallAccepted, поле старта `Lndh→Llhh`), из-за чего вызовы `RingbackTonePlayer.stop()` тихо не накладывались. Восстановлено: `stop()` строго перед `Connection.destroy()` и в `onCallAccepted()`.

### 📜 Прокрутка чата под невидимкой / умной античиталкой
Патчи scroll-to-bottom ссылались на старый класс `ara`/поле `Lpze;->g:J` → в стоке стали `bta`/`Lv2f;->g:J`. Якоря обновлены, долистывание восстановлено.

### 🔒 Восстановлены нейтрализации телеметрии
VK Push analytics (`nvj→szj`, ранний возврат `kotlin.Unit`) и латентный DPS-пинг (`gq5→cs5`, `V()` → return-void) снова нейтрализованы.

### 🔑 Chat-PIN — управление в приложении (long-press)
Долгое нажатие на заголовок чата переключает защиту (Toast-подтверждение). chatId берётся из навигационного делегата `ChatScreen->p:Lxv;` (ключ `"id"`), long-press реализован на `aac.onTouchEvent` стока, обёрнут в try/catch.

### 🛠 Инфраструктура: аудит применённости патчей
Детектор молчаливо-непримененных патчей (`work/sentinel_audit.py`): sentinel-grep + сверка R8-якорей с чистым стоком (`baksmali_pristine`). Правило: прогон при каждой новой версии стока.

---

## 🆕 Унаследовано из 26.27.1 V1
- 🔐 «О приложении» → настройки мода (long-press восстановлен).
- 🔑 Chat-PIN — вход по PIN (регистрация `ChatPinReceiver` в манифесте подтверждена).
- 🛡 Обход App Lock через переоткрытие устранён (`onStart`-гейт, sentinel `applock_onstart_v1`).

## 🆕 Унаследовано из 26.26.0 V5
- ❌ Визуальная метка удалённых сообщений (антиудаление, красный крестик + setAlpha 0.5f).
- 📜 Форс-консьюм при открытии чата (`MessagesListWidget.onAttach`, гейт `smartAntiRead || antiRead`).

---

## 🛡 Профиль приватности

### Невидимка (`invisible`) — мастер-переключатель

| Дочерний флаг | Что делает |
|---|---|
| `antiTyping` | собеседник не видит набор текста |
| `antiRead` | собеседник не видит отметку «прочитано» |
| `smartAntiRead` | «прочитано» уходит только после вашего ответа (consume-on-use) |
| `antiPresence` | онлайн / last-seen скрыты |

### Антиудаление (`antiDelete`) — 4 слоя
1. **L3** — Central WS chat-event. 2. **L4** — Room ORM body-clear. 3. **Маркер** ❌ + время удаления. 4. **FCM офлайн-персист**.

### Нейтрализация телеметрии — чокпоинты
`p0_telem_push` (VK Push, восстановлен в V2), `p0_telem_onelog`, `p0_telem_operator` (IP-strip), `p0_telem_dps` (восстановлен в V2).

---

## 🔍 Верификация (V3)

Независимая адверсариальная кросс-верификация — декомпиляция **фактически собранного** `MAX_26.27.1_arm64_V3.apk` (baksmali, 3 DEX, 25 010 smali-файлов), проверка по байткоду:

| Проверка | Результат |
|---|---|
| chatPin тумблер рендерится в UI (`AppFlagsView` const-string «🔒 PIN на отдельные чаты» + `addSwitchRow`, без hidden) | ✅ подтверждено |
| drag: `PinHandleTouch.onTouch` возвращает `0x1` на ACTION_DOWN после `pinStartDrag` | ✅ подтверждено |
| ringback `stop()` перед `Connection.destroy()` в `qe1` + в `h75.onCallAccepted()` | ✅ подтверждено |
| телеметрия: VK Push ранний `kotlin.Unit`, DPS `V()`→`return-void` первой инструкцией | ✅ подтверждено |
| chat-pin гейт: `ChatScreen.onViewCreated` → `sget-boolean AppFlags->chatPin:Z` + `if-eqz` | ✅ подтверждено |
| `UpdateChecker.currentVersion()` = `"26.27.1.3"` (не .2) | ✅ подтверждено |
| `apksigner verify` (все 4 APK) | ✅ v2+v3, exact ABI, no tracer, distinct SHA |
| Динамический smoke на устройстве | **не проводился** (устройство недоступно; публикация под ответственность мейнтейнера). Chat-PIN UI и перетаскивание закреплённых рекомендуется проверить на устройстве. |

---

## 📦 Артефакты (V3)

| Файл | SHA-256 |
|---|---|
| `MAX_26.27.1_arm64_V3.apk` | `d1c51825a7c9ab2cbf3ebe7242583bbb97c9d2561add45815faadc7073fc4ba7` |
| `MAX_26.27.1_arm64_V3_clone.apk` | `3e09f2a1d84bb11d4bc7466a478e9367a0a00b32adc0b8724f6310fe7ca4a0ba` |
| `MAX_26.27.1_arm7_V3.apk` | `b88b1d14639cbaf01b8566343abf6ebe824eb34f199f33c6485dabe4d9946450` |
| `MAX_26.27.1_arm7_V3_clone.apk` | `5d99fa90a179d0050819bd8e49399c7fb09cf1f0ea470e09b75effbf74912885` |

Подпись: v2+v3 APK Signature Scheme. Cert SHA-1 идентичен всей линейке 26.x — install -r без потери данных.

---

*Аудит выполнен статическим анализом smali (baksmali + независимый агент-верификатор) с декомпиляцией фактически собранных APK. Динамическое тестирование на устройстве для сборки 26.27.1 V3 не проводилось; chat-pin UI (long-press заголовка) и перетаскивание закреплённых чатов (за pin-иконку) требуют проверки на устройстве.*
