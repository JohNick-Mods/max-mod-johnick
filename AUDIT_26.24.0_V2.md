# MAX 26.24.0 RS V2 — Технический аудит мода

**Версия стока**: 26.24.0 (источник — RuStore, RS variant)
**Версия мода**: V2 (Privacy Mod by JohNick)
**Дата**: 2026-07-25
**Совместимость**: `ru.oneme.app` (Android 8.0–16.0 / API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

> **26.24.0 RS V2** — UI-рефактор диалогов поверх V1 (порт из донор-сборки MAX_26.24.0.64): закруглённые диалоги, нативные радиокнопки выбора цвета иконки, вынесенный класс `DialogsUtils`, переписанный на ~63% экран настроек мода. **Безопасность и приватность не менялись** — все security-инварианты идентичны V1. Функционал V1 полностью сохранён.

---

## 🆕 Новое в 26.24.0 RS V2 — UI-рефактор диалогов

### 🔵 Закруглённые диалоги
Все диалоги мода (меню категорий настроек, каждая категория, confirm дев-меню, «Проверить обновления», «Разрешить ссылки max.ru») получили закруглённые углы **16dp** через `GradientDrawable`-подложку окна (best-effort, обёрнуто в try/catch — при любой ошибке возвращается обычный диалог). Фон подложки — по системной теме.

### 🎨 Выбор цвета иконки — нативные RadioButton
Раньше 6 цветов выбирались текстовыми псевдо-маркерами ◉/○ (TextView). Теперь — нативные Android `RadioButton` в `RadioGroup` (`setId 0..5`, `check(iconColor)`, `OnCheckedChangeListener`). Выбор применяется на лету и сохраняется в prefs `app_flags` (ключ `iconColor`), переживает перезапуск (применяется в `AppFlags.init`).

### 🧩 Новый класс `DialogsUtils` (c1)
Тема-детект и закруглённый показ вынесены в отдельный util-класс `Lone/me/util/DialogsUtils;` (`getDialogTheme(Context)I` + `showRoundedDialog(Builder,Context)AlertDialog`). Раньше логика дублировалась инлайном в 6 местах — теперь единая точка. Cross-DEX вызовы (c2 дев-меню → c1) корректны в multidex.

### 📐 `AppFlagsView` переписан (−63% кода)
Экран настроек мода факторизован: switch-строки строятся хелпером `addSwitchRow(Context,key,label,state)`, показ — `showDialog(Context,View,title)`. Размер класса сократился **97 КБ → 36 КБ**. Разметка диалогов исправлена. Все 31 видимый тумблер, 5 категорий и каскад «Невидимки» сохранены без изменений.

### 🌗 Тема диалогов — по системному night-mode (R8-независимо)
Определение тёмной/светлой темы переведено на `Configuration.uiMode & UI_MODE_NIGHT_MASK` (как в AMOLED-патче) — **R8-независимо**, не ломается при обновлении стока. Ранее использовался обфусцированный theme-singleton MAX, чьи R8-имена дрейфовали каждый bump; под 26.24.0 они протухли — источник латентной деградации устранён.

> Все изменения V2 — только в UI-слое (`c1`: DialogsUtils, AppFlagsView, UpdateChecker, AppLinkPrompt; `c2`: дев-меню gk2). Сеть, телеметрия, приватность, подпись — без изменений относительно V1.

---

## 🔧 База V1 (порт под сток 26.24.0)

- **Порт стока 26.24.0** из RuStore, **R8-rebump 47 классов** (drift 26.23.2 → 26.24.0 закрыт).
- **`filterSettingsList`** переписан под новую sealed-архитектуру (`Lxdf;`/`Lwaf;` + `Lq6h;`).
- **`filterChatList` / `filterPageList`** мигрированы на новые типы (`Ln13;→Lc33;`, `Les6;→Ltv6;`).
- **`startOnNew`** — новый chokepoint `c1/wi3.smali`.
- **`hide_phone` Gate2** пере-отрезолвлен (`b94 → db4 → tc7 → cgf`).
- **`manual_update_check` + long-press дев-меню** re-enabled под новую раскладку «О приложении».
- **`resources.arsc`** — icon-switcher stable-ids под 26.24.0.

---

## 🐞 Сохранено из V1 / 26.23.2 V1

- 🗑 **Секунды и иконка корзины на удалённых** — безусловно на рендере (`ЧЧ:ММ:СС ДД.ММ` + 🗑).
- 🔢 **Счётчик непрочитанных** не накручивается на холодном старте.
- 🎨 **«Цвет иконки» — 6 цветов**, меняется на лету (в V2 — через RadioButton).
- 🕹 **«Невидимка» — единый мастер-тумблер** каскадно управляет `antiRead`/`antiTyping`/`antiPresence`.
- 🛡 **Антиудаление** — 4 слоя + офлайн-персист, нет push об удалённых, ⏱→✓.
- ⌚ **Watch-mode** (< 500 px), 🌑 **AMOLED-тема** (авто при тёмной).
- 📲 **Push у клона стабилен**, 🔎 **проверка/автопроверка обновлений**, 🎛 **дев-меню MAX**, 📱 **SMS-регистрация**.

---

## 🛡 Профиль приватности

### Невидимка (`invisible`) — мастер-переключатель

| Дочерний флаг | Что делает |
|---|---|
| Антипечатанье (`antiTyping`) | собеседник не видит набор текста |
| Античиталка (`antiRead`) | собеседник не видит отметку «прочитано» |
| Умная античиталка (`smartAntiRead`) | «прочитано» уходит только ПОСЛЕ вашего ответа |
| Антиприсутствие (`antiPresence`) | онлайн / last-seen скрыты (независимый тумблер) |

**Взаимоисключение**: `antiRead` и `smartAntiRead` не могут быть включены одновременно.

### Анти-удаление (`antiDelete`) — 4 слоя + офлайн-персист
1. **L3 — Central WS chat-event**. 2. **L4 — Room ORM body-clear**. 3. **V3 persist**. 4. **Маркер 🗑 + время**. 5. **Офлайн-удаления через FCM** восстанавливаются на старте. Восстановленные сохраняются с `isRead=1`.

---

## 📋 Переключатели в UI

Все по умолчанию **OFF** (кроме `amoledTheme` — авто) — до первого включения мод идентичен стоку (антидетект). Диалог настроек следует системной теме, углы закруглены. Открытие: **Настройки → О приложении → long-press по строке версии**.

**Приватность (10):** `antiDelete`, `invisible` (+ `antiTyping`/`antiRead`/`smartAntiRead`/`antiPresence`), `blockMobileIdVerify`, `hidePhoneNumber`, `hideDigitalId`, `gateOpcode5`.
**Папки и вкладки (7):** `hideAll`, `hideSferum`, `hideFavorites`, `hideBusinessPromo`, `hideInviteFriends`, `reorderTabs`, `startOnNew`.
**Сервисные чаты (9):** `hideVerifyCodes`, `hideMaxOfficial`, `hideInteresting`, `hideUsefulNotifs`, `hideMaxBusiness`, `hidePromoWidgets`, `hideInviteLinks`, `hideChannels`, `hideChannelRecs`.
**Звонки и система (5):** `autoStart`, `backCameraForNote`, `debugMenu`, `gateOpcode5`, `amoledTheme`.
**Внешний вид (1):** `iconColor` (6 цветов, нативный RadioGroup).

---

## 🔒 Security-инварианты (идентичны V1 — V2 их не касался)

### 🌐 DNS-блокировка телеметрии (P0)
```
trace-flow.ru
sdk-api.apptracer.ru
tracker-api.vk-analytics.ru
tracker.my.com
top-fwz1.mail.ru
```

### 🔕 Приваси-стабы (P0/P1)
- **myTracker (VK)** — нейтрализован (`return-void`).
- **Tracer-инициализаторы (7×)** — застаблены (SDK не стартует).
- **Firebase Installations** — `getComponents() → emptyList()`.
- **Фоновая выгрузка контактов** — нейтрализована.
- **NFC-HCE** — APDU-ответ `SW 6F00`.
- **TrustManager accept-all** — заменён на `throw`.
- **No-IP телеметрия** — IP не отправляется в телеметрию соединения.

### ✍️ Подпись
- **apksigner**, схемы **v2 + v3** (без v1). Cert **SHA-1 `d6a7d757…1298b`** — идентичен для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.

### 📦 Нативные библиотеки
Удаляется **только `libtracernative.so`**. Остальные `.so` — оригинальные из стока 26.24.0.

---

## ⚠ Честно: ограничения в 26.24.0 RS V2

- **AMOLED — покрытие ~95%**: список чатов, окно чата и bottom-sheets могут остаться тёмно-серыми.
- **`#FILE-1`**: PDF / видео / m4a из основного аккаунта временами отправляются с проблемами.
- **Закруглённые диалоги — best-effort**: у отдельных системных диалогов (не AlertDialog мода) углы могут остаться прямыми.
- **Тумблер `improveNotifications` скрыт из UI**.
- **«Цифровой ID» и «Госуслуги баннер»** — один `hideDigitalId`.
- **Требуется one-time `pm clear ru.oneme.ap2`** после установки APK клона.
- **Минимальная версия Android — 8.0** (API 26).

---

## ✅ Smoke-тест (2026-07-25)

- Устройство: Samsung S25 (`RFCX5013V5F`), Android 16.
- Основной (`ru.oneme.app`) + клон (`ru.oneme.ap2`) — **0 FATAL, 0 VerifyError, 0 ClassCast**.
- **ART dex2oat `--compiler-filter=verify` → status=PERFORMED** (весь DEX верифицирован без ошибок).
- `install -r` поверх 26.23.0 → user-data сохранена, чистый старт, оба процесса живы.
- Проверено на устройстве:
  - **Закруглённые диалоги** — меню категорий, каждая категория, дев-меню, проверка обновлений, разрешение ссылок ✅.
  - **RadioButton выбора цвета иконки** — нативные радиокнопки, выбор меняет иконку и переживает перезапуск ✅.
  - **Тема диалогов** — следует системной (тёмная/светлая), углы закруглены в обоих ✅.
  - **AppFlagsView** — все категории, тумблеры, каскад «Невидимки» без регрессий ✅.
  - **DialogsUtils** — загружается и верифицируется ART чисто (cross-DEX c2→c1) ✅.

---

## 🔒 SHA-256 (V2)

```
c9e452bd45b5729e43efd124dcdc6da0fd2338d023bbd73b3094e4ad4b38873c  MAX_26.24.0_arm64_V2.apk
497330c3588065b8a75a3a2c96c955e070bbb1cceab974e7d723f621a36a42c1  MAX_26.24.0_arm64_V2_clone.apk
77c192757085931bd5d2e83df9b7f510ce00aa61170e7e85b2369eefa276bbd4  MAX_26.24.0_arm7_V2.apk
dd7d3b2c46198ec724c47ce8cbf40f5d9426a75330bb4f1e538a054a8206cb91  MAX_26.24.0_arm7_V2_clone.apk
```

---

## 📜 Лицензия

Только для личного использования, в учебных и исследовательских целях. Мод предоставляется «as is», подписан debug-ключом, не публикуется в Google Play. Правообладатель оригинального MAX — **ООО «МАХ» (VK)**; мод не аффилирован, не претендует на права на код / знаки / иконки. Использование может нарушать Условия сервиса — **на свой риск**.
