# MAX 26.22.1 V1 — Технический аудит мода

**Версия стока**: 26.22.1 (build 6772)
**Версия мода**: V1 (Privacy Mod by JohNick)
**Дата**: 2026-07-10
**Совместимость**: `ru.oneme.app` (Android 8.0–16.0 / API 26–36) · arm64-v8a + armeabi-v7a · основной + клон (`ru.oneme.ap2`)

---

## 🛡 Профиль приватности

### Невидимка (`invisible`) — мастер-переключатель
Взводит дочерние приватные флаги; каждый работает и отдельно.

| Дочерний флаг | Что делает |
|---|---|
| Антипечатанье (`antiTyping`) | собеседник не видит набор текста |
| Античиталка (`antiRead`) | собеседник не видит отметку «прочитано» |
| Умная античиталка (`smartAntiRead`) | «прочитано» уходит только ПОСЛЕ вашего ответа |
| Антиприсутствие (`antiPresence`) | онлайн / last-seen скрыты (сервер видит offline) |

**Взаимоисключение**: `antiRead` и `smartAntiRead` не могут быть включены одновременно — включение одного снимает другой.

### Анти-удаление (`antiDelete`) — 4 слоя + офлайн-персист

1. **L3 — Central WS chat-event**: live-канал, собеседник удаляет при онлайн-приложении.
2. **L4 — Room ORM body-clear**: локальный SQL-сброс тела блокируется.
3. **V3 persist**: удалённые сообщения остаются видимыми после ре-синка.
4. **Маркер «· удалено»** в строке времени + 🆕 **время удаления** (`dk_times`).
5. 🆕 **Офлайн-удаления через FCM** — если удаление пришло, пока приложение убито OEM-killer'ом (RealMe/ColorOS/MIUI: «Очистить всё», «Force stop», hard-kill), метка восстанавливается при следующем старте (msgId заносится в персистентный список на входе FCM-пуша `MessageRemoved`).

Общий гейт `antiDelete`: **ON** → защита; **OFF** → поведение стока.

---

## 📋 Переключатели — 29 тумблеров

Все по умолчанию **OFF** — до первого включения мод ведёт себя идентично стоку (антидетект). Диалог настроек следует системной теме.

**Приватность (9):** `antiDelete`, `invisible` (+ `antiTyping` / `antiRead` / `smartAntiRead` / `antiPresence`), `blockMobileIdVerify`, `hidePhoneNumber`, `hideDigitalId`.

**Папки и вкладки (7):** `hideAll`, `hideSferum`, `hideFavorites`, `hideBusinessPromo`, `hideInviteFriends`, `reorderTabs`, `startOnNew`.

**Сервисные чаты (7):** `hideVerifyCodes`, `hideMaxOfficial`, `hideInteresting`, `hideUsefulNotifs`, `hideMaxBusiness`, `hideInviteLinks`, `hideChannels` («Только чаты»).

**Звонки и система (6):** `autoStart`, `improveNotifications` 🆕, `backCameraForNote`, `debugMenu`, `gateOpcode5` 🆕, [+1 существующий].

---

## 🆕 Новое в 26.22.1 (относительно 26.22.0 V1)

**Скрытие:**
- 🆕 **Скрыт промо-баннер «Госуслуги»** — в 26.22.0 `hideDigitalId` убирал только пункт «Цифровой ID» из списка Настроек, но промо-баннер вверху экрана оставался. Теперь при `hideDigitalId=ON` баннер тоже уходит.
- 🆕 **`hideChannels` работает в кастомных папках** — в 26.22.0 каналы скрывались только в главном списке чатов; в пользовательских папках оставались. Добавлен второй chokepoint фильтрации (folder-scoped список).

**Анти-удаление:**
- 🆕 **Метка «удалено» переживает жёсткий kill приложения** — на OEM-прошивках (RealMe/ColorOS/MIUI и т.п.), где системный «Очистить всё» или Force Stop убивал приложение до записи метки. Теперь удаления, пришедшие через фоновый FCM-push (`MessageRemoved`), восстанавливаются при следующем старте.
- 🆕 **Время удаления** в метке (не только сам факт).

**Уведомления и транспорт (оба под тумблерами OFF-default):**
- 🆕 **`improveNotifications`** — отдельный тихий канал (`IMPORTANCE_LOW`) для фоновой службы «keep-alive». В стоке foreground-нотификация и push-сообщения сидят на одном канале — если пользователь глушит фоновый канал в системе или включает DND, вместе с ним заглушаются и push сообщений. При ON фоновая служба уходит на свой канал, push сообщений остаётся на основном.
- 🆕 **`gateOpcode5`** — блокирует принудительный logout от сервера. При получении WS-команды «сбросить все задачи» (opcode 5) клиент её игнорирует, сохраняя сессию.

**Стабильность:**
- Push на клон (`ru.oneme.ap2`) стабильно приходят между сессиями — устранена ротация device_id, которая в V1 26.22.0 могла обнулять push-endpoint при нескольких холодных стартах. После установки клона по-прежнему требуется одноразовый `Настройки → Приложения → MAX Clone → Очистить данные`, дальше не нужен.
- SMS-регистрация нового номера на клоне работает штатно (устранён боковой эффект защитного гейта).

---

## 🔓 Always-on (без тумблера)

- Отправка любых файлов (обход клиентского блэклиста + маскировка `.bin` для сервера).
- Изоляция device_id клона (клон получает свой push).
- Подавление ложного красного «!» при Невидимке.
- SMS-авторизация при регистрации работает.
- Скриншоты и запись экрана разрешены везде (`FLAG_SECURE` снят).
- IP-адрес не отправляется в телеметрию соединения.
- Автоповорот следует настройке устройства.
- Диалог «Разрешить ссылки max.ru» (one-guard) — App Links для Цифрового ID.
- TrustManager accept-all → отклоняется; logcat-dump → выключен; фоновая выгрузка контактов → выключена; NFC-HCE → SW 6F00; myTracker (VK) → выключен.
- Антиудаление (4 слоя + офлайн-персист), tracer-stubs, AppFlags init, KeepAlive, dev-меню MAX по long-press, dynamic FileProvider authority для клона.

---

## ⚠ Честно: чего НЕТ в 26.22.1 V1

- **2 транспортные нейтрализации остаются DEFERRED** до 26.23.x:
  - `WS-redirect` — используется штатно для sharding SMS-регистрации по диапазону номера; безусловный skip ломает вход по новым номерам. В 26.23.x — под тумблер OFF-default с UI-предупреждением.
  - `vmk` (SSL-session-validate / DNS-client-config) — реестр `PmsProperty` изменил тип default'ов, тривиальная замена вызывает `ClassCast`. Требует передекодировки getter'ов.
- **Требуется one-time `pm clear ru.oneme.ap2`** после установки APK клона (штатный шаг установки клона).

---

## 🔐 Безопасность и сборка

- **Подпись**: apksigner v2 + v3 (debug.jks), cert SHA-1 `d6a7d757…1298b` — одинаков для всех сборок 26.x → `install -r` поверх любой предыдущей версии мода без потери данных.
- **DEX**: 3 шт. (c1/c2/c3), сторонние библиотеки — оригинальные из стока 26.22.1.
- **Телеметрия**: только дефолтная MAX (ничего не добавляется). DNS-diff 26.22.0→26.22.1 — 0 новых хостов.
- **Permissions**: без изменений vs 26.22.0 (41 permission — идентичны).
- **PmsProperty реестр**: без новых серверных флагов (394 идентичны — только переобфускация R8).
- **R8-карта**: пере-резолв всех anchor-имён под сток 26.22.1 (build 6772). 23 переезда классов + 15 типов полей.

## ✅ Smoke-тест (2026-07-10)

- Устройство: Samsung S25 (RFCX5013V5F), Android 15.
- Основной (`ru.oneme.app`) — 0 FATAL, 0 VerifyError, 0 ClassCast.
- Клон (`ru.oneme.ap2`) — 0 FATAL, 0 VerifyError, 0 ClassCast (после штатного `pm clear` при первой установке).
- Проверено: скрытие Госуслуги-баннера ✅, скрытие каналов в кастомных папках ✅, метка «удалено» после Force Stop / «Очистить всё» ✅, время удаления в метке ✅, push на клон между несколькими холодными стартами ✅, `improveNotifications=ON` не роняет foreground-службу на Android 12+ (LOW-канал) ✅, `gateOpcode5=ON` не сбрасывает сессию ✅, SMS-регистрация нового номера на клоне ✅.

### 🐞 Исправлено в этой сборке

- **`improveNotifications` крах на Android 12+** — отдельный канал для фоновой службы был создан с `IMPORTANCE_MIN`, а Android 12+ реджектит foreground-нотификацию на MIN-канале (`CannotPostForegroundServiceNotificationException`). Канал переведён на `IMPORTANCE_LOW` — всё ещё без звука/вибро, но принимается системой.
- **NPE-защита в FCM-хендлере анти-удаления** — добавлена проверка результата парсинга push-map (null-guard), чтобы повреждённый push не роняет обработчик.

---

## 🔒 SHA-256 (V1)

```
a2d36cc0d3564ae95152caeaab719faf9e48910a152191bcbdc462c0ff128582  MAX_26.22.1_arm64_V1.apk
a107f972ec7fcd3b30d8af8dc1fff1e389c08a1894c902cd32f878b3d3c68f6f  MAX_26.22.1_arm64_V1_clone.apk
8e8276c5821f9dae94797b5278cb9182d426934e181c3527a75b7fc103e73bfc  MAX_26.22.1_arm7_V1.apk
9ff858685668f7eff761b3611e62c40f3193c2a425db85e36f499c56a41aff71  MAX_26.22.1_arm7_V1_clone.apk
```

---

## 📜 Лицензия

Только для личного использования, в учебных и исследовательских целях. Мод предоставляется «as is», подписан debug-ключом, не публикуется в Google Play. Правообладатель оригинального MAX — **ООО «МАХ» (VK)**; мод не аффилирован, не претендует на права на код / знаки / иконки. Использование может нарушать Условия сервиса — **на свой риск**.
